# Initial Issues List

1. ~~Printable version of the signature form to replace built-in signature fields~~
2. ~~Notice at the end explaining what happens next (committee reviews, contacts for payment if approved)~~
3. ~~Examples of good and bad photography — real comparison photos wired in for all 10 requested shots, each with both a good and bad pair~~
4. ~~Signing their own name digitally is fine~~
5. ~~Remove color-coded plan or key checkbox~~
6. ~~Don't hide the photo requirements modal~~
7. ~~Drawing on the plot plan should be draggable~~
8. ~~Hook up to Google Maps API or similar, have property align with grid~~
9. ~~On acknowledgements checkbox 1, add links to those resources~~
10. ~~Research the First Service Residential Connect API~~
11. ~~Change Carol Taylor to CarolMarie Taylor throughout; change "How to Submit" to "How to submit signature forms"~~
12. ~~Include checks on email and phone number formatting~~
13. ~~"Email to committee" shouldn't automatically invoke the print dialogue~~
14. ~~Reformat the output as a 1-page~~

## Sprint log

- **Sprint 0 (2026-07-02)** — baseline housekeeping, complete. Committed the outstanding grid-painter/auto-align working tree as a clean anchor; rewrote `README.md` (correct `master` branch, current feature description, points to CLAUDE.md/ROADMAP.md); moved the source PDF to `docs/source-form.pdf` and updated its mention in CLAUDE.md. Next: Sprint 1 (packet assembly & submission ending).
- **Sprint 1 (2026-07-02)** — packet assembly & submission ending, complete. Added Section 08 "Review & Submit": a live "Your packet" ✓/✗ list (form PDF, plot plan, each requested photo by name, signed neighbor form, sketches) plus a guided two-step finish (Save PDF → Open pre-addressed email) that replaces the old three-button submit bar. Emailing now soft-gates on packet completeness (missing-items modal with explicit override), and the mailto body carries an attachment manifest so applicant and reviewer can both verify the packet. Section 06's plot/photos/neighbor rows became live derived status rows (sketches + fee stay manual); `collect()` derives `req_plot`/`req_photos`/`req_neighbors` from real app state. JSON export moved to the sidenav; both email buttons now validate via one shared flow. Verified in-browser: packet list/Section 06 rows react to the photo questionnaire, soft-gate lists per-photo gaps, no console errors. Untested by automation (needs the user's eyes): the actual print-dialog Save-as-PDF and the mailto handoff to a mail client. Next: Sprint 2 (workflow temporality & friction).
- **Sprint 2 (2026-07-02)** — workflow temporality & friction, complete except #3 (blocked on real images from the user). Added "save and come back" pause-notes to Sections 04/05 (honest that the browser can't hold attached files between visits); replaced the Draw step's nine-tool intro paragraph with a one-line per-tool hint above the canvas (copy lives on `TOOL_MODES[].hint`); the progress meter now counts three packet items (plot, all requested photos, neighbor form) so 100% is unreachable with an empty packet; `startBuilder()`'s empty-address error paints a field error and scrolls to/focuses the address; `#ack-date` defaults to today; and wizard steps 2–4 (plus the Konva-failure notice) share a `data-switch-upload` bail-out that returns to step 1 with the upload panel open, preserving parcel/drawing state. Verified in-browser (fresh profile, no console errors): hint renders, ack-date defaults, pause-notes present, bail-out lands on step 1 with upload selected and builder dots hidden, empty-address error paints. Needs the user's eyes: hint-line legibility while actually drawing, and the bail-out feel from a real mid-wizard session. Next: Sprint 3 (robustness & external-dependency hardening).
- **Sprint 3 (2026-07-03)** — robustness & external-dependency hardening, complete. Vendored the two runtime libraries into `assets/vendor/` as exact pinned builds (`konva-10.3.0.min.js` — was the floating `konva@10` tag — plus `maplibre-gl-5.23.0.js/.css`; Google Fonts stays CDN). Autosave failures are now visible: `saveDraft()` toggles a persistent `#save-warning` sidenav indicator on quota/blocked errors and clears it on the next good save. County-GIS calls all go through `gisFetch()` (10s timeout) and hard failures show `#gis-fallback-msg` in wizard step 2 with an "upload instead" route; `startBuilder()` likewise routes to upload if MapLibre itself never loaded. Draft lifecycle: after the email step, Section 08 reveals a cleanup panel offering to delete the saved draft (PII incl. the signature image); `deleteDraft()` also removes the legacy `.v3`/`.v2` keys (previously "Clear" left them behind and the one-time migration resurrected the old draft on reload); `plot.version` bumped to 4 in lockstep with `DRAFT_KEY` (restore accepts 3–4). Verified in-browser: vendored libs load with no console errors; the save-warning appears when `localStorage.setItem` throws and clears on recovery; all new elements hidden by default. Needs the user's eyes: the GIS fallback notice under a real outage (or DevTools offline mode in the Select step), and the post-submit cleanup panel after a real email finish. Next: Sprint 4 (code health — extract pure geometry + unit tests).
- **Sprint 4 (2026-07-03)** — code health, complete. Two-stage refactor of the 3,200-line `app.js` IIFE: (4a) extracted all pure geometry into `assets/geometry.js` — parcel projection, `fitSimilarity`, `computeAutoAlignBearing` (road direction now a parameter), and the flood-fill core `computeFloodFill` — with a 29-case `node --test` suite (`tests/geometry.test.js`) covering grid sizing/clamps, similarity-fit recovery, auto-align flips, and the flood-fill wall rules (axis boxes, diagonal >50%-coverage fills, shared-edge preservation, seed-side selection); deleted dead `pointInPolygon`; added a deps-free `package.json` (`"type":"module"`) so Node parses the modules. (4b) split the app into native ES modules — `utils.js`, `geometry.js`, `plot-editor.js` (Konva surface + PALETTE/TOOL_MODES), `map-wizard.js` (map/GIS/rotation + exported live-binding state), `app.js` (entry: form, packet, persistence, outputs) — with function-only import cycles back to the entry (documented in CLAUDE.md gotchas). **Bug found & fixed during the split:** `restoreDraft()` never re-set `selectedParcelGeoJSON`, so the first autosave after a restore silently wrote `parcelCoords: null` and the parcel grid failed to rebuild on the following reload; restore now re-adopts the parcel via `restoreParcelFromDraft()`. Verified: `node --check` on all five modules, 29/29 tests, browser smoke test (module load with zero console errors, landing → form, all generated content, autosave → draft → restore round-trip). Needs the user's eyes: the full map wizard (Select → Orient → Draw), drawing tools incl. fill-against-walls, and a draft restore that includes a real parcel + drawn plan. Next: Sprint 5 (accessibility & mobile).
- **Sprint 5 (2026-07-03)** — accessibility & mobile, complete pending user verification. **Typed-signature alternative:** the owner signature has a Draw/Type toggle; Type reveals a text input whose typed full legal name acts as the electronic signature — validated, counted by the progress meter, persisted (`ownerSigMethod`/`ownerTypedSignature` in the draft, additive so `.v4` drafts stay compatible), and rendered in the preview ("typed signature") and print PDF ("signed electronically by typing name"). **Latent bug found & fixed:** `SignaturePad` sized its canvas while the form layout was still `display:none` (0×0 rect → 1×1 backing store), so ink drawn before any window-resize event landed on a one-pixel canvas; `resize()` now no-ops while hidden, `enterForm()` sizes the pads on reveal, and a draft restore that arrives while the pad is hidden parks the dataURL in `pending` until the next real resize (`toDataURL()` passes it through so round-trips don't lose ink). **Touch:** map drag-to-rotate converted from mouse to pointer events (pointer capture + `pointercancel`; step 3 forces `touch-action:none` on the map canvas so the browser can't hijack the drag); the plot surface gained two-finger pinch-zoom/pan — one finger keeps using the active tool, a second finger cancels the in-progress gesture and rolls a half-painted stroke back through its undo point; `@media (pointer: coarse)` bumps palette/zoom/step-dot/rotate-fine buttons to fingertip size and heightens the signature pad. **Keyboard review outcome:** the Orient map is a focus stop in step 3 (arrows rotate 1°, Shift+arrow 15°, aria-live bearing readout); parcel *selection* (map click) and freehand *drawing* are inherently pointer gestures and stay that way — the upload path is the complete keyboard-only route (native controls end-to-end), every builder step has a focusable "upload instead" bail-out, and the typed signature removes the last pointer-only *required* interaction in the form. `node --check` clean on all edited modules; 29/29 geometry tests. Needs the user's eyes (ideally on a real phone): pinch/pan/one-finger draw on the plot stage, touch drag-to-rotate in Orient, the Draw/Type signature toggle incl. a draft round-trip, and a keyboard-only pass (Tab through; sign by typing).
- **Sprint 6 (2026-07-03)** — Draw-step UX: explicit completion + compact layout, complete pending user verification. **Explicit completion:** a primary "Done — use this plan" CTA at the bottom of step 4 sets a `plotConfirmed` flag (plot-editor owns it via `isPlotConfirmed()`/`setPlotConfirmed()`); build-mode `plotProvided()` is now `plotUsed() && isPlotConfirmed()`, so a single stray stroke no longer marks the plan "provided" — the Section 06 row, packet list, and email soft-gate show an amber "In progress — mark it finished in Section 02" third state (`.reqstat.is-partial`) for drawn-but-unconfirmed plans, and the progress meter counts the plot only when confirmed. The flag persists as `plot.confirmed` (additive to `.v4`), is cleared by Clear plan and by a parcel/orientation rebuild, survives further edits, flips the Draw dot to done (surviving `showStep()`'s dot re-render), and Done scrolls on to Section 03. **Compact layout:** tools became an icon-only vertical rail docked to the canvas's left edge with Undo/Redo below a rule (labels live on title/aria-label and lead the hint text; rail flips horizontal ≤640px; fingertip-sized on coarse pointers); materials are the single chip row above the canvas; zoom (+/−/Fit/%) moved onto the canvas as a top-right MapLibre-style overlay; a persistent "1 square = 1 ft" badge sits bottom-left; the Back/Front Yard labels became on-canvas overlays; and one dark status strip under the canvas merges the tool hint, a contextual brush control (visible only while Paint is active), and the mouse-glyph bar — the standing controls row, the separate hint line, and the step-4 hotkey bar are gone, and the grid-wrap now exactly matches the canvas box so the aerial-loading overlay lost its special-case sizing. **Navigation:** "Back to orientation" deleted (the step dots are the back-nav); step 4 ends in Clear plan / Done. **Keyboard undo:** Ctrl/Cmd+Z, Ctrl+Shift+Z, Ctrl+Y while the plot host is on screen, suppressed while typing in inputs/textareas; undo/redo now refresh the packet UI directly since keyboard fires no pointerup. Also fixed the `clearPlot()` confirm copy (Clear IS undoable — it says so now). `node --check` clean on all edited modules; 29/29 geometry tests. Needs the user's eyes: the new step-4 layout at desktop + narrow widths, the Done flow end-to-end (draw → Done → Section 06/08 states → draft reload keeps the confirmation), the rebuilt confirm-clearing on a parcel/orientation change, and Ctrl+Z while drawing. Next: Sprint 7 (stamps, legend, custom materials).
- **Sprint 7 (2026-07-03)** — Draw-step content: stamps, legend, custom materials, complete pending user verification. **Stamp tool:** a new data-driven `STAMPS` constant (nine plan symbols — tree, palm, shrub, cactus/agave, xeriscape groundcover, boulder, camera, yard light, AC unit — each an SVG path in a 24-box plus a real-world footprint in feet) drives a new Stamp tool in the rail: pick a glyph in the status strip's contextual slot (same slot as Paint's brush control), click the plan to place it. Each placement is a `kind:"stamp"` Konva group — black glyph over a white halo so it reads on both aerial and painted fills, plus an invisible full-footprint hit disc so Select/Erase catch clicks anywhere inside the symbol — and Select-drag, Erase-click, undo/redo, draft persistence, and print/preview all came free from the existing drawLayer vector pipeline. Placement arms on press and commits on release so a second finger (pinch) cancels it, matching the paint-stroke rollback behavior; a translucent ghost previews the true footprint under the cursor. **Palette:** Tree / Yard Light / Camera chips removed from `PALETTE` (they're point objects, not area materials) — their ids live on in `RETIRED_MATERIALS`, still resolvable by `PALETTE_MAP`, so old drafts render and the legend names them. **Auto legend:** new `plotLegend()` export reports the materials actually painted plus the stamps actually placed; `buildPreview()` and `buildPrintHTML()` now render a swatch/glyph + name key under the plan image (print styles inline in the print doc). **Custom materials:** a dashed "+ Add material" chip opens an inline popover (name + native color input; Enter adds without submitting the form); customs join the chip row and `PALETTE_MAP`, persist as `plot.customMaterials` (additive to `.v4`), and restore merges them back in *before* `loadCells()` repaints. Colors brighter than 220/255 perceived brightness are rejected with a warning — the multiply blend over the aerial washes near-whites out to invisible. `node --check` clean on both edited modules; 29/29 geometry tests. Needs the user's eyes: glyph legibility at real zoom levels (the nine paths are tuned by eye, edit `STAMPS[].d`/`ft` to taste), stamp place/move/erase/undo plus a draft round-trip, the legend in preview + printed PDF (incl. a retired-id draft), and the custom-material flow end-to-end. Sprint 7 closes the 2026-07-03 Draw-step review; next is decision-gated backlog (real submission backend) or ISSUES #3 content.
- **Post-Sprint-7 (2026-07-03)** — plant icon catalog + ISSUES #3 closed out. **Stamp catalog swap:** replaced the 6 hand-drawn plant/landscape glyphs (tree, palm, shrub, cactus/agave, xeriscape groundcover, boulder) with a 24-icon full-color plant/landscape set (a downloaded "HOA plant icon starter set", vendored into `assets/stamps/*.svg`) rendered as `Konva.Image` stamps at their real-world footprint; the 3 hardware symbols (camera, yard light, AC unit) stay as the original path-stamp glyphs — kept alongside the new catalog per explicit user choice rather than dropped. Added `rehydrateStampImages()`: Konva can't serialize an `Image` node's actual bitmap, so draft restore, undo/redo, and print's offscreen composite all re-attach the correct `<img>` element from a preloaded `STAMP_IMG_CACHE` after rebuilding from JSON. **Bug found & fixed:** `activeStamp` still defaulted to the removed `"tree"` id, leaving the Stamp tool inert (silent no-op) until a picker button was clicked; defaults to `"canopy_tree"` now. **ISSUES #3 closed:** `material_sample` (paint chip / material sample) was the last shot without real examples; added a real Benjamin Moore chip (good — sharp, flat, legible) and a blurry angled chip with glare (bad); also caught and fixed a `back_left`/`back_right` file mixup from an earlier pass, and replaced the placeholder `back_left`/`back_right` pair with real yard photos. All 10 requested shots now have real good/bad photo pairs. `node --check` clean on `plot-editor.js`/`app.js`; 29/29 geometry tests (unaffected, run as a sanity check). Needs the user's eyes: the new plant icon picker/glyphs at real canvas zoom, a stamp place/undo/restore round-trip with the new image stamps, and the print/preview legend rendering the new icons.
- **Sprint 9 (2026-07-04)** — itemized improvements list (Section 03 rework), complete. Section 03 was a single free textarea whose placeholder merely *hoped* the applicant supplied the materials / dimensions / example pictures the committee's own template demands ("Must Include…") — so a packet incomplete by the HOA's definition sailed through the soft gate. Now Section 03 is an **improvements repeater** (neighbor-list pattern): each row has an **action** (Add / Replace / Remove), a short **name**, **materials & color**, **dimensions**, and a file input for an **example/catalog picture**. Choosing *Remove* hides the materials field and relabels the picture as an optional "what's being removed" shot (`applyImprovementAction`); the per-row remove button never empties the list. The old textarea is **demoted** to an optional "Anything else the committee should know" notes field — `required` moved to the item list (each row's name is `[required]`; the always-present starter row forces at least one). **Packet integration:** `improvementChecklist()` (Add/Replace items with a name) feeds the packet list (a new "Example / catalog pictures — N of M attached" row with per-item subrows), `packetMissingList()`, the email soft gate, the mailto attachment manifest, and a 4th progress-meter slot (lenient — a Remove-only list needs no pictures). **Outputs:** a shared `improvementsTableHTML()` renders the items as a table (action / item / materials / dimensions / picture filename) + the notes beneath, in both the preview modal and the print PDF (replacing the prose `.print-proposal` blob). **Persistence:** `items[]` is additive to `.v4` (older drafts keep the starter row and land their `proposal` text in the notes field); picture filenames restore as `is-prior` (browser can't repopulate a file input, same as photos). Verified in-browser (fresh port + a restored draft): no console errors; add/remove, the Remove-action toggle, the last-row guard, the packet row, an `items[]`+notes draft round-trip (incl. the Remove row's hidden materials on reload), and the preview table all correct. `node --check` clean on app.js; 29/29 geometry tests. Next: decision-gated backlog (real submission backend) or the Sprint 9 v2 nicety (improvements-list ↔ plot legend cross-check).
- **Sprint 8 (2026-07-03)** — journey de-duplication, complete. A user-journey redundancy audit found the packet reviewed twice (Section 06's derived rows duplicated Section 08's packet list, which then sent users *back* to 06 for the sketches checkbox), the application fee explained three ways (before review / after submission / after approval), and "What Happens Next" restating the submit instructions with a stale "(see dates below)" pointer. Fixes: Section 06 retired (sketches checkbox now lives directly under the packet list in Review & Submit; `refreshRequirementRows`/`.reqstat` deleted; sections renumbered 07→06, 08→07), fee story canonicalized to "nothing due until approved" everywhere (no-op `req_fee` checkbox and the contradicting landing/print-footer lines removed), acks 3–4 gained muted cross-references to the packet checklist / Section 05 sample photo, and What Happens Next lost its redundant step 1 while "(see dates below)" became a working link back to the landing. Verified in-browser on a fresh port: no console errors, packet row/gate/preview/draft round-trip all correct from the checkbox's new home. Deliberately kept: the paired pause-notes (04/05), triple contact info on the landing screen, re-typing the name as the typed e-signature, and the preview-modal/finish-step parallel paths (both funnel through the same flows).
- **Sprint 10 (2026-07-04)** — sequence the packet endgame (journey reorder), complete. From the 2026-07-04 journey discussion. **Reorder:** Improvements moved ahead of Site/Plot (name the change, *then* place it on the lot — and the two lightest steps now precede the heavy map+draw wizard), Photos up one, Adjacent Owners down one; new order `01` Applicant · `02` Improvements · `03` Site/Plot · `04` Photos · `05` Adjacent Owners · `06` Acknowledgments · `07` Review & Submit. Sidenav / card numbers / DOM order updated in lockstep; every "Section 0X" copy reference across `app.js` + `index.html` remapped; the Draw-step "Done" CTA now scrolls **forward** to Photos instead of back to Improvements. **Split the neighbor step:** Adjacent Owners is now the **roster only** (names/addresses — the data that pre-addresses the printed form); the physical act — `#print-neighbor-form`, the `#neighbor-form-file` re-attach, `#neighbor-filelist`, and the multi-day `.pause-note` — moved into Section 07, because neighbors sign *against the printed packet*, which doesn't exist until the end. **Section 07 = a 3-step Print → Sign → Transmit finish:** (1) Save & print the packet (the record neighbors sign against, and your copy to keep), (2) Collect neighbors' signatures (print form → collect in person → attach the scan), (3) Email the packet ("keep your printed packet for your records"). The neighbor packet row links to `#finish-step-2`; the soft-gate/manifest copy points at Section 07 Step 2; the orphaned `.neighbor-form-actions` CSS was removed (`.btn` already sets the pointer cursor). Verified in-browser (fresh port 8137): DOM order + card numbers + sidenav all read 01→07; three finish steps render 1/2/3 with the neighbor form under `#packet` and absent from `#neighbors`; no duplicate ids; packet list intact; zero console errors on load. `node --check` clean on app.js/map-wizard.js/plot-editor.js; 29/29 geometry tests. Needs the user's eyes: the reordered flow top-to-bottom feel, and the neighbor print/attach from its new home in the finish steps. Next: Sprint 11 (improvement categories with per-category fields).
- **Sprint 11 (2026-07-04)** — improvement categories with per-category fields, complete. From the 2026-07-04 discussion: a plant doesn't need "materials & color" or "12′ × 24′ dimensions", and paint is a color code, not dimensions — but Section 02 asked all of it for every item. Added a **`CATEGORIES` data constant** (Structure / Hardscape / Plants and landscaping / Paint or exterior color / Equipment / Pool, spa or water feature / Other), each declaring how the two flexible fields (materials, dimensions) are **labeled** and whether dimensions shows (`dims: null` hides it). Each improvement row gained a **category `<select>`**; `applyImprovementSchema()` (generalizes the old `applyImprovementAction`) relabels/hides the two slots per category and still lets **Remove** hide materials + relabel the picture. Name moved to its own full-width line; a CSS `:has()` rule spans materials across the row when dimensions is hidden. `improvementItems()` now emits `category`; `improvementsTableHTML()` gained a **Type** column and category-aware n/a (Remove → materials n/a, paint → dimensions n/a); the draft stores `category` (additive on Sprint 9's `items[]`, old drafts default `structure`) and restore re-applies the schema; Section 02 sub-copy reworded. Verified in-browser (fresh port): the 7-option select toggles field labels/visibility correctly (landscape → "Plant type & quantity"; paint → "Color name, code & manufacturer" + dims hidden + materials spans the row; Remove → materials hidden), `category` round-trips through a save+reload with the schema re-applied, and the preview table renders the Type column with correct n/a cells; zero console errors. `node --check` clean on app.js; 29/29 geometry tests. Deliberately deferred (backlog): per-category *required*-field enforcement — the picture soft-gate stays uniform since a catalog/example picture is reasonable for every category. Next: decision-gated backlog (real submission backend) or the Sprint 9 v2 nicety (improvements ↔ plot legend cross-check, now sharper with categories — only structure/hardscape/pool are expected to show on the plan).
- **Post-Sprint-11 (2026-07-04)** — attachment ergonomics, committed `0dce60a`/`ddddc91`. Drag-and-drop + clipboard-paste for every file input via a new leaf module `assets/dropzone.js` (`registerDropzone`: DataTransfer + synthetic input/change events so downstream listeners run unchanged; accept-mismatch shake; appends on `multiple`; paste routes hovered → focused → single-visible zone); photo attachments render framed thumbnails with Replace/Remove (blob-URL cache revoked on replace/remove); sidenav Clear moved into a danger-zone treatment. Roadmap extended with Sprints 12–16 from the 2026-07-04 issue dump (incl. the Sprint 15 submission-architecture research verdicts: Cloudflare Worker+R2 is the zero-cost path; Cloudinary and form relays ruled out).
- **Sprint 12 (2026-07-04)** — quick fixes: numbering, redundancy, tool chrome, complete. **Work-area close-ups are area-agnostic and multi-file** (issue 14): the shot moved out of the back-yard group into a standalone `PHOTO_CLOSEUP` constant + its own photo group shown whenever any area is checked, titled "Close-up(s) of the work area(s)"; the input is `multiple` (dropzone appends, picker replaces — neighbor-form semantics), thumbnails render one card per file with per-file Remove (FileList rebuilt via DataTransfer), and the draft's `photos` value for it is a filename list (restore/preview/print/manifest all handle string-or-array; the `back_closeup` id is kept so old drafts restore unchanged). **Improvement rows renumber** on every add/remove (`renumberImprovements()` — display labels from DOM position, ids stay monotonic). **The manual sketches attestation is gone** (checkbox, `sketchesConfirmed()`, gate/packet/mailto/SUB_LABELS entries, restore loop, orphaned CSS) — Section 02 itemizes all of it now, so every packet row is derived; old drafts' `req_sketches` restores into nothing, same as `req_fee`. **Instant tooltips** cover the full draw chrome: stamp picker switched from native `title` to `data-tip` with an up-flowing tooltip, Undo/Redo likewise (Redo advertises Ctrl+Shift+Z). **`ICON.fill` is the user-supplied 48-box bucket glyph** restyled to rail conventions (stroke 3.8 ≈ neighbors' visual weight); `cursorForMode()` now reads each icon's viewBox instead of assuming 24, scaling the cursor halo/ink strokes to match. Verified in-browser (fresh port 8141, zero console errors incl. a draft-restore load): closeup group visibility, 2-file attach/count/thumbs, per-file remove, packet "1 of 4" + subrow filenames, list-shaped draft + plural is-prior restore, renumbering incl. remove-all re-add, data-tips present with no titles, 48-box fill icon. `node --check` clean; 29/29 geometry tests. Needs the user's eyes: tooltip hover-feel on the rail/stamp picker and the Fill tool's glyph cursor at real zoom. Next: Sprint 13 (eraser & object lifecycle).
- **Print-first journey rework (2026-07-05)** — user decision after debugging "Open pre-addressed email does nothing": the app-side flow was verified correct end-to-end, but the machine's `mailto:` handler (Windows new-Outlook AppX) silently no-ops even for a trivial short link fired from the OS shell — and the generated URL (~1.7 KB on a near-empty form) would exceed Windows' ~2 KB external-protocol ceiling on any real application anyway. Rather than harden an unfixable handoff, **the journey now ends at the printable packet**: one "Save & print" finish step; `buildPrintHTML()` emits three pages — submission instructions (page 1: steps, the specialist's email, derived include-list + still-outstanding warning, deadline/fee/assistance), the application, and the adjacent-owner signature form (last page, six blank wet-signature rows, change summary from the Section 02 items). Removed: the email flow (`EMAIL_TO`/`openMailto`/`startEmailFlow`/`finishEmail`, both email buttons), the separate `printNeighborForm()` window, the `#neighbor-form-file` re-attach loop, and the whole Adjacent Owners roster section (sections renumbered: 05 Acknowledgments, 06 Review & Submit; old drafts' `neighbors`/`neighborForm` keys restore harmlessly). Progress meter now counts three packet slots; the advisory review gate stays, serving the print path. `node --check` clean. Needs the user's eyes: the printed 3-page packet (instructions content, page breaks, signature-form layout) and the reworked Section 06.
- **Baked parcel extract + community-center start view (2026-07-05)** — external-dependency reduction, user-requested. New `tools/fetch-parcels.mjs` bakes `assets/parcels.json` (7,517 parcels — every polygon + situs address in the streets.json bbox, from county layer 50 PARCELS_CREST via 4 paged queries; 3.7 MB raw / ~650 KB gzipped, coords rounded to 6 decimals, APN-sorted for stable diffs). map-wizard.js is now **local-first for all parcel data**: `locateByAddress` matches number+name+city against the baked records (zero county round-trips for in-area addresses), `loadParcels` filters the extract by envelope (318 parcels / 2.4 ms for the standard 600 m box — and the baked records carry their addresses, so the follow-up assessor label query is gone too), and `fetchParcelGeometryByAPN` checks the extract before fetching; `bakedParcelFeature()` rebuilds records into the exact Feature shape the live queries return so everything downstream is source-agnostic. Live county endpoints remain as fallback (out-of-bbox addresses, extract load failure, envelopes past the baked edge). The map now opens on the **community's center of mass** ([-117.035692, 33.953955], mean of all baked parcel centroids, stored as `parcels.json`'s `center` with `HOA_CENTER` as pre-load fallback) — the old constant was downtown Beaumont, ~5 km east of the actual tracts. Remaining live-at-runtime: OpenFreeMap basemap tiles, county aerial (Orient tiles + Draw backdrop export), Overpass (out-of-bbox streets only), Google Fonts. `node --check` clean; 31/31 geometry tests. Needs the user's eyes: address lookup feel (should be instant), parcel labels/click-select from baked data, map's initial framing, and a DevTools-offline run of Select with the county blocked.
- **Print-packet overhaul planned (2026-07-05)** — new user request list turned into ROADMAP Sprints 17–22 (ahead of the still-open 14–16): (17) drop the Preview modal, drive the paged.js render from a Save/Print header; (18) IndexedDB attachment persistence via vendored idb-keyval so photos survive a reload; (19) client-side image-compression budget (25 MB target) + dedicated print pages for Improvements/Site-Plot/Photos with scaled uncropped images + multi-photo-per-improvement; (20) rename Section 06 to "Save & Print Packet" and reframe its checks from packet artifacts to workflow-step status; (21) redesign the cover (concise, review-dates in a right rail, neighbor signatures at the bottom); (22) Adobe Acrobat Sign text-tag fields on the saved PDF. Pivotal decision logged in the roadmap preamble: stay on paged.js (Path A, recommended — best-effort compression + Adobe text tags) vs. generate the PDF with pdf-lib (Path B — hard size cap + real AcroForm fields, but a large rewrite).
- **Path A locked + Sprint 17 (2026-07-05)** — user locked **Path A** (stay on paged.js; Path B/pdf-lib kept only as a documented escalation). **Sprint 17 complete:** dropped the "Preview first" modal — it looked nothing like the paged.js print. Deleted `#preview-modal` + `buildPreview()`/`photoPreviewHTML()`/`ownerSigHTML`/`sigImg`/`yn`, the `#do-print` button, the "Preview first" `type=submit` button, the form-submit→preview handler (now a bare `preventDefault`), and the `openModal`/`closeModal`/preview-Escape wiring; `SUB_LABELS`/`improvementsTableHTML`/`plotLegendHTML` stayed (print-only callers). `printPreview()` now injects a `@media screen`-only `.print-bar` toolbar into the render popup (Save as PDF / Print / Close), hidden in `@media print`; the auto-`window.print()` is gone — buttons drive it and enable once `PagedConfig.after` fires (5s timer + `<script onerror>` fallback enables them if paged.js can't load, so the native `@page` flow still prints). CLAUDE.md Output-paths / Guided-finish / plot-surface passages updated. Left behind (deferred): dead preview-only CSS in styles.css. `node --check` clean; 31/31 geometry tests. Needs the user's eyes: the popup toolbar (button enable-after-pagination, Save-as-PDF destination, Close) — the print popup isn't reliably drivable from this agent's browser automation.
