# Initial Issues List

1. ~~Printable version of the signature form to replace built-in signature fields~~
2. ~~Notice at the end explaining what happens next (committee reviews, contacts for payment if approved)~~
3. ~~Examples of good and bad photography — real comparison photos wired in for all 10 requested shots, each with both a good and bad pair~~
4. ~~Signing their own name digitally is fine~~
5. ~~Remove color-coded plan or key checkbox~~
6. ~~Don't hide the photo requirements modal~~
7. ~~Drawing on the plot plan should be draggable~~
8. ~~Hook up to Google Maps API or similar, have property align with grid~~
9. ~~On acknowledgements checkbox 1, add links to those resources~~
10. ~~Research the First Service Residential Connect API~~
11. ~~Change Carol Taylor to CarolMarie Taylor throughout; change "How to Submit" to "How to submit signature forms"~~
12. ~~Include checks on email and phone number formatting~~
13. ~~"Email to committee" shouldn't automatically invoke the print dialogue~~
14. ~~Reformat the output as a 1-page~~

## Sprint log

- **Printable-packet design & accessibility pass (2026-07-11, user-prioritized ad hoc)** — audited the paged.js packet after the user flagged undersized text, an under-filled cover, and the missing demo patio-cover image. Reworked the print stylesheet to use explicit physical sizes (11pt body; 10pt support; 9pt secondary; 8pt running header/footer), darkened low-contrast gray copy, and turned the cover's linear flex stack into a real two-column middle region (four-step submission flow beside a prominent email-only/deadline card and owner certification). The six adjacent-owner rows grew from 33px to 0.46in for handwriting. Improvement pictures now render in a 2-column grid at 2.35in high instead of 150px thumbnails. `printImg()` now emits contextual alt text for improvement evidence, the plot, and property photos; the generated document declares English; in-form attachment thumbnails name the file. Patio-cover root cause was deployment state, not image decoding or attachment code: commit `2551564` added the valid 1000×800 JPEG and its `demo-data.json` reference, but `origin/master` still pointed to `fb7ce3b`, two commits behind local HEAD. `node --check assets/app.js` and `node --test` clean (61/61).

- **Sprint 0 (2026-07-02)** — baseline housekeeping, complete. Committed the outstanding grid-painter/auto-align working tree as a clean anchor; rewrote `README.md` (correct `master` branch, current feature description, points to CLAUDE.md/ROADMAP.md); moved the source PDF to `docs/source-form.pdf` and updated its mention in CLAUDE.md. Next: Sprint 1 (packet assembly & submission ending).
- **Sprint 1 (2026-07-02)** — packet assembly & submission ending, complete. Added Section 08 "Review & Submit": a live "Your packet" ✓/✗ list (form PDF, plot plan, each requested photo by name, signed neighbor form, sketches) plus a guided two-step finish (Save PDF → Open pre-addressed email) that replaces the old three-button submit bar. Emailing now soft-gates on packet completeness (missing-items modal with explicit override), and the mailto body carries an attachment manifest so applicant and reviewer can both verify the packet. Section 06's plot/photos/neighbor rows became live derived status rows (sketches + fee stay manual); `collect()` derives `req_plot`/`req_photos`/`req_neighbors` from real app state. JSON export moved to the sidenav; both email buttons now validate via one shared flow. Verified in-browser: packet list/Section 06 rows react to the photo questionnaire, soft-gate lists per-photo gaps, no console errors. Untested by automation (needs the user's eyes): the actual print-dialog Save-as-PDF and the mailto handoff to a mail client. Next: Sprint 2 (workflow temporality & friction).
- **Sprint 2 (2026-07-02)** — workflow temporality & friction, complete except #3 (blocked on real images from the user). Added "save and come back" pause-notes to Sections 04/05 (honest that the browser can't hold attached files between visits); replaced the Draw step's nine-tool intro paragraph with a one-line per-tool hint above the canvas (copy lives on `TOOL_MODES[].hint`); the progress meter now counts three packet items (plot, all requested photos, neighbor form) so 100% is unreachable with an empty packet; `startBuilder()`'s empty-address error paints a field error and scrolls to/focuses the address; `#ack-date` defaults to today; and wizard steps 2–4 (plus the Konva-failure notice) share a `data-switch-upload` bail-out that returns to step 1 with the upload panel open, preserving parcel/drawing state. Verified in-browser (fresh profile, no console errors): hint renders, ack-date defaults, pause-notes present, bail-out lands on step 1 with upload selected and builder dots hidden, empty-address error paints. Needs the user's eyes: hint-line legibility while actually drawing, and the bail-out feel from a real mid-wizard session. Next: Sprint 3 (robustness & external-dependency hardening).
- **Sprint 3 (2026-07-03)** — robustness & external-dependency hardening, complete. Vendored the two runtime libraries into `assets/vendor/` as exact pinned builds (`konva-10.3.0.min.js` — was the floating `konva@10` tag — plus `maplibre-gl-5.23.0.js/.css`; Google Fonts stays CDN). Autosave failures are now visible: `saveDraft()` toggles a persistent `#save-warning` sidenav indicator on quota/blocked errors and clears it on the next good save. County-GIS calls all go through `gisFetch()` (10s timeout) and hard failures show `#gis-fallback-msg` in wizard step 2 with an "upload instead" route; `startBuilder()` likewise routes to upload if MapLibre itself never loaded. Draft lifecycle: after the email step, Section 08 reveals a cleanup panel offering to delete the saved draft (PII incl. the signature image); `deleteDraft()` also removes the legacy `.v3`/`.v2` keys (previously "Clear" left them behind and the one-time migration resurrected the old draft on reload); `plot.version` bumped to 4 in lockstep with `DRAFT_KEY` (restore accepts 3–4). Verified in-browser: vendored libs load with no console errors; the save-warning appears when `localStorage.setItem` throws and clears on recovery; all new elements hidden by default. Needs the user's eyes: the GIS fallback notice under a real outage (or DevTools offline mode in the Select step), and the post-submit cleanup panel after a real email finish. Next: Sprint 4 (code health — extract pure geometry + unit tests).
- **Sprint 4 (2026-07-03)** — code health, complete. Two-stage refactor of the 3,200-line `app.js` IIFE: (4a) extracted all pure geometry into `assets/geometry.js` — parcel projection, `fitSimilarity`, `computeAutoAlignBearing` (road direction now a parameter), and the flood-fill core `computeFloodFill` — with a 29-case `node --test` suite (`tests/geometry.test.js`) covering grid sizing/clamps, similarity-fit recovery, auto-align flips, and the flood-fill wall rules (axis boxes, diagonal >50%-coverage fills, shared-edge preservation, seed-side selection); deleted dead `pointInPolygon`; added a deps-free `package.json` (`"type":"module"`) so Node parses the modules. (4b) split the app into native ES modules — `utils.js`, `geometry.js`, `plot-editor.js` (Konva surface + PALETTE/TOOL_MODES), `map-wizard.js` (map/GIS/rotation + exported live-binding state), `app.js` (entry: form, packet, persistence, outputs) — with function-only import cycles back to the entry (documented in CLAUDE.md gotchas). **Bug found & fixed during the split:** `restoreDraft()` never re-set `selectedParcelGeoJSON`, so the first autosave after a restore silently wrote `parcelCoords: null` and the parcel grid failed to rebuild on the following reload; restore now re-adopts the parcel via `restoreParcelFromDraft()`. Verified: `node --check` on all five modules, 29/29 tests, browser smoke test (module load with zero console errors, landing → form, all generated content, autosave → draft → restore round-trip). Needs the user's eyes: the full map wizard (Select → Orient → Draw), drawing tools incl. fill-against-walls, and a draft restore that includes a real parcel + drawn plan. Next: Sprint 5 (accessibility & mobile).
- **Sprint 5 (2026-07-03)** — accessibility & mobile, complete pending user verification. **Typed-signature alternative:** the owner signature has a Draw/Type toggle; Type reveals a text input whose typed full legal name acts as the electronic signature — validated, counted by the progress meter, persisted (`ownerSigMethod`/`ownerTypedSignature` in the draft, additive so `.v4` drafts stay compatible), and rendered in the preview ("typed signature") and print PDF ("signed electronically by typing name"). **Latent bug found & fixed:** `SignaturePad` sized its canvas while the form layout was still `display:none` (0×0 rect → 1×1 backing store), so ink drawn before any window-resize event landed on a one-pixel canvas; `resize()` now no-ops while hidden, `enterForm()` sizes the pads on reveal, and a draft restore that arrives while the pad is hidden parks the dataURL in `pending` until the next real resize (`toDataURL()` passes it through so round-trips don't lose ink). **Touch:** map drag-to-rotate converted from mouse to pointer events (pointer capture + `pointercancel`; step 3 forces `touch-action:none` on the map canvas so the browser can't hijack the drag); the plot surface gained two-finger pinch-zoom/pan — one finger keeps using the active tool, a second finger cancels the in-progress gesture and rolls a half-painted stroke back through its undo point; `@media (pointer: coarse)` bumps palette/zoom/step-dot/rotate-fine buttons to fingertip size and heightens the signature pad. **Keyboard review outcome:** the Orient map is a focus stop in step 3 (arrows rotate 1°, Shift+arrow 15°, aria-live bearing readout); parcel *selection* (map click) and freehand *drawing* are inherently pointer gestures and stay that way — the upload path is the complete keyboard-only route (native controls end-to-end), every builder step has a focusable "upload instead" bail-out, and the typed signature removes the last pointer-only *required* interaction in the form. `node --check` clean on all edited modules; 29/29 geometry tests. Needs the user's eyes (ideally on a real phone): pinch/pan/one-finger draw on the plot stage, touch drag-to-rotate in Orient, the Draw/Type signature toggle incl. a draft round-trip, and a keyboard-only pass (Tab through; sign by typing).
- **Sprint 6 (2026-07-03)** — Draw-step UX: explicit completion + compact layout, complete pending user verification. **Explicit completion:** a primary "Done — use this plan" CTA at the bottom of step 4 sets a `plotConfirmed` flag (plot-editor owns it via `isPlotConfirmed()`/`setPlotConfirmed()`); build-mode `plotProvided()` is now `plotUsed() && isPlotConfirmed()`, so a single stray stroke no longer marks the plan "provided" — the Section 06 row, packet list, and email soft-gate show an amber "In progress — mark it finished in Section 02" third state (`.reqstat.is-partial`) for drawn-but-unconfirmed plans, and the progress meter counts the plot only when confirmed. The flag persists as `plot.confirmed` (additive to `.v4`), is cleared by Clear plan and by a parcel/orientation rebuild, survives further edits, flips the Draw dot to done (surviving `showStep()`'s dot re-render), and Done scrolls on to Section 03. **Compact layout:** tools became an icon-only vertical rail docked to the canvas's left edge with Undo/Redo below a rule (labels live on title/aria-label and lead the hint text; rail flips horizontal ≤640px; fingertip-sized on coarse pointers); materials are the single chip row above the canvas; zoom (+/−/Fit/%) moved onto the canvas as a top-right MapLibre-style overlay; a persistent "1 square = 1 ft" badge sits bottom-left; the Back/Front Yard labels became on-canvas overlays; and one dark status strip under the canvas merges the tool hint, a contextual brush control (visible only while Paint is active), and the mouse-glyph bar — the standing controls row, the separate hint line, and the step-4 hotkey bar are gone, and the grid-wrap now exactly matches the canvas box so the aerial-loading overlay lost its special-case sizing. **Navigation:** "Back to orientation" deleted (the step dots are the back-nav); step 4 ends in Clear plan / Done. **Keyboard undo:** Ctrl/Cmd+Z, Ctrl+Shift+Z, Ctrl+Y while the plot host is on screen, suppressed while typing in inputs/textareas; undo/redo now refresh the packet UI directly since keyboard fires no pointerup. Also fixed the `clearPlot()` confirm copy (Clear IS undoable — it says so now). `node --check` clean on all edited modules; 29/29 geometry tests. Needs the user's eyes: the new step-4 layout at desktop + narrow widths, the Done flow end-to-end (draw → Done → Section 06/08 states → draft reload keeps the confirmation), the rebuilt confirm-clearing on a parcel/orientation change, and Ctrl+Z while drawing. Next: Sprint 7 (stamps, legend, custom materials).
- **Sprint 7 (2026-07-03)** — Draw-step content: stamps, legend, custom materials, complete pending user verification. **Stamp tool:** a new data-driven `STAMPS` constant (nine plan symbols — tree, palm, shrub, cactus/agave, xeriscape groundcover, boulder, camera, yard light, AC unit — each an SVG path in a 24-box plus a real-world footprint in feet) drives a new Stamp tool in the rail: pick a glyph in the status strip's contextual slot (same slot as Paint's brush control), click the plan to place it. Each placement is a `kind:"stamp"` Konva group — black glyph over a white halo so it reads on both aerial and painted fills, plus an invisible full-footprint hit disc so Select/Erase catch clicks anywhere inside the symbol — and Select-drag, Erase-click, undo/redo, draft persistence, and print/preview all came free from the existing drawLayer vector pipeline. Placement arms on press and commits on release so a second finger (pinch) cancels it, matching the paint-stroke rollback behavior; a translucent ghost previews the true footprint under the cursor. **Palette:** Tree / Yard Light / Camera chips removed from `PALETTE` (they're point objects, not area materials) — their ids live on in `RETIRED_MATERIALS`, still resolvable by `PALETTE_MAP`, so old drafts render and the legend names them. **Auto legend:** new `plotLegend()` export reports the materials actually painted plus the stamps actually placed; `buildPreview()` and `buildPrintHTML()` now render a swatch/glyph + name key under the plan image (print styles inline in the print doc). **Custom materials:** a dashed "+ Add material" chip opens an inline popover (name + native color input; Enter adds without submitting the form); customs join the chip row and `PALETTE_MAP`, persist as `plot.customMaterials` (additive to `.v4`), and restore merges them back in *before* `loadCells()` repaints. Colors brighter than 220/255 perceived brightness are rejected with a warning — the multiply blend over the aerial washes near-whites out to invisible. `node --check` clean on both edited modules; 29/29 geometry tests. Needs the user's eyes: glyph legibility at real zoom levels (the nine paths are tuned by eye, edit `STAMPS[].d`/`ft` to taste), stamp place/move/erase/undo plus a draft round-trip, the legend in preview + printed PDF (incl. a retired-id draft), and the custom-material flow end-to-end. Sprint 7 closes the 2026-07-03 Draw-step review; next is decision-gated backlog (real submission backend) or ISSUES #3 content.
- **Post-Sprint-7 (2026-07-03)** — plant icon catalog + ISSUES #3 closed out. **Stamp catalog swap:** replaced the 6 hand-drawn plant/landscape glyphs (tree, palm, shrub, cactus/agave, xeriscape groundcover, boulder) with a 24-icon full-color plant/landscape set (a downloaded "HOA plant icon starter set", vendored into `assets/stamps/*.svg`) rendered as `Konva.Image` stamps at their real-world footprint; the 3 hardware symbols (camera, yard light, AC unit) stay as the original path-stamp glyphs — kept alongside the new catalog per explicit user choice rather than dropped. Added `rehydrateStampImages()`: Konva can't serialize an `Image` node's actual bitmap, so draft restore, undo/redo, and print's offscreen composite all re-attach the correct `<img>` element from a preloaded `STAMP_IMG_CACHE` after rebuilding from JSON. **Bug found & fixed:** `activeStamp` still defaulted to the removed `"tree"` id, leaving the Stamp tool inert (silent no-op) until a picker button was clicked; defaults to `"canopy_tree"` now. **ISSUES #3 closed:** `material_sample` (paint chip / material sample) was the last shot without real examples; added a real Benjamin Moore chip (good — sharp, flat, legible) and a blurry angled chip with glare (bad); also caught and fixed a `back_left`/`back_right` file mixup from an earlier pass, and replaced the placeholder `back_left`/`back_right` pair with real yard photos. All 10 requested shots now have real good/bad photo pairs. `node --check` clean on `plot-editor.js`/`app.js`; 29/29 geometry tests (unaffected, run as a sanity check). Needs the user's eyes: the new plant icon picker/glyphs at real canvas zoom, a stamp place/undo/restore round-trip with the new image stamps, and the print/preview legend rendering the new icons.
- **Sprint 9 (2026-07-04)** — itemized improvements list (Section 03 rework), complete. Section 03 was a single free textarea whose placeholder merely *hoped* the applicant supplied the materials / dimensions / example pictures the committee's own template demands ("Must Include…") — so a packet incomplete by the HOA's definition sailed through the soft gate. Now Section 03 is an **improvements repeater** (neighbor-list pattern): each row has an **action** (Add / Replace / Remove), a short **name**, **materials & color**, **dimensions**, and a file input for an **example/catalog picture**. Choosing *Remove* hides the materials field and relabels the picture as an optional "what's being removed" shot (`applyImprovementAction`); the per-row remove button never empties the list. The old textarea is **demoted** to an optional "Anything else the committee should know" notes field — `required` moved to the item list (each row's name is `[required]`; the always-present starter row forces at least one). **Packet integration:** `improvementChecklist()` (Add/Replace items with a name) feeds the packet list (a new "Example / catalog pictures — N of M attached" row with per-item subrows), `packetMissingList()`, the email soft gate, the mailto attachment manifest, and a 4th progress-meter slot (lenient — a Remove-only list needs no pictures). **Outputs:** a shared `improvementsTableHTML()` renders the items as a table (action / item / materials / dimensions / picture filename) + the notes beneath, in both the preview modal and the print PDF (replacing the prose `.print-proposal` blob). **Persistence:** `items[]` is additive to `.v4` (older drafts keep the starter row and land their `proposal` text in the notes field); picture filenames restore as `is-prior` (browser can't repopulate a file input, same as photos). Verified in-browser (fresh port + a restored draft): no console errors; add/remove, the Remove-action toggle, the last-row guard, the packet row, an `items[]`+notes draft round-trip (incl. the Remove row's hidden materials on reload), and the preview table all correct. `node --check` clean on app.js; 29/29 geometry tests. Next: decision-gated backlog (real submission backend) or the Sprint 9 v2 nicety (improvements-list ↔ plot legend cross-check).
- **Sprint 8 (2026-07-03)** — journey de-duplication, complete. A user-journey redundancy audit found the packet reviewed twice (Section 06's derived rows duplicated Section 08's packet list, which then sent users *back* to 06 for the sketches checkbox), the application fee explained three ways (before review / after submission / after approval), and "What Happens Next" restating the submit instructions with a stale "(see dates below)" pointer. Fixes: Section 06 retired (sketches checkbox now lives directly under the packet list in Review & Submit; `refreshRequirementRows`/`.reqstat` deleted; sections renumbered 07→06, 08→07), fee story canonicalized to "nothing due until approved" everywhere (no-op `req_fee` checkbox and the contradicting landing/print-footer lines removed), acks 3–4 gained muted cross-references to the packet checklist / Section 05 sample photo, and What Happens Next lost its redundant step 1 while "(see dates below)" became a working link back to the landing. Verified in-browser on a fresh port: no console errors, packet row/gate/preview/draft round-trip all correct from the checkbox's new home. Deliberately kept: the paired pause-notes (04/05), triple contact info on the landing screen, re-typing the name as the typed e-signature, and the preview-modal/finish-step parallel paths (both funnel through the same flows).
- **Sprint 10 (2026-07-04)** — sequence the packet endgame (journey reorder), complete. From the 2026-07-04 journey discussion. **Reorder:** Improvements moved ahead of Site/Plot (name the change, *then* place it on the lot — and the two lightest steps now precede the heavy map+draw wizard), Photos up one, Adjacent Owners down one; new order `01` Applicant · `02` Improvements · `03` Site/Plot · `04` Photos · `05` Adjacent Owners · `06` Acknowledgments · `07` Review & Submit. Sidenav / card numbers / DOM order updated in lockstep; every "Section 0X" copy reference across `app.js` + `index.html` remapped; the Draw-step "Done" CTA now scrolls **forward** to Photos instead of back to Improvements. **Split the neighbor step:** Adjacent Owners is now the **roster only** (names/addresses — the data that pre-addresses the printed form); the physical act — `#print-neighbor-form`, the `#neighbor-form-file` re-attach, `#neighbor-filelist`, and the multi-day `.pause-note` — moved into Section 07, because neighbors sign *against the printed packet*, which doesn't exist until the end. **Section 07 = a 3-step Print → Sign → Transmit finish:** (1) Save & print the packet (the record neighbors sign against, and your copy to keep), (2) Collect neighbors' signatures (print form → collect in person → attach the scan), (3) Email the packet ("keep your printed packet for your records"). The neighbor packet row links to `#finish-step-2`; the soft-gate/manifest copy points at Section 07 Step 2; the orphaned `.neighbor-form-actions` CSS was removed (`.btn` already sets the pointer cursor). Verified in-browser (fresh port 8137): DOM order + card numbers + sidenav all read 01→07; three finish steps render 1/2/3 with the neighbor form under `#packet` and absent from `#neighbors`; no duplicate ids; packet list intact; zero console errors on load. `node --check` clean on app.js/map-wizard.js/plot-editor.js; 29/29 geometry tests. Needs the user's eyes: the reordered flow top-to-bottom feel, and the neighbor print/attach from its new home in the finish steps. Next: Sprint 11 (improvement categories with per-category fields).
- **Sprint 11 (2026-07-04)** — improvement categories with per-category fields, complete. From the 2026-07-04 discussion: a plant doesn't need "materials & color" or "12′ × 24′ dimensions", and paint is a color code, not dimensions — but Section 02 asked all of it for every item. Added a **`CATEGORIES` data constant** (Structure / Hardscape / Plants and landscaping / Paint or exterior color / Equipment / Pool, spa or water feature / Other), each declaring how the two flexible fields (materials, dimensions) are **labeled** and whether dimensions shows (`dims: null` hides it). Each improvement row gained a **category `<select>`**; `applyImprovementSchema()` (generalizes the old `applyImprovementAction`) relabels/hides the two slots per category and still lets **Remove** hide materials + relabel the picture. Name moved to its own full-width line; a CSS `:has()` rule spans materials across the row when dimensions is hidden. `improvementItems()` now emits `category`; `improvementsTableHTML()` gained a **Type** column and category-aware n/a (Remove → materials n/a, paint → dimensions n/a); the draft stores `category` (additive on Sprint 9's `items[]`, old drafts default `structure`) and restore re-applies the schema; Section 02 sub-copy reworded. Verified in-browser (fresh port): the 7-option select toggles field labels/visibility correctly (landscape → "Plant type & quantity"; paint → "Color name, code & manufacturer" + dims hidden + materials spans the row; Remove → materials hidden), `category` round-trips through a save+reload with the schema re-applied, and the preview table renders the Type column with correct n/a cells; zero console errors. `node --check` clean on app.js; 29/29 geometry tests. Deliberately deferred (backlog): per-category *required*-field enforcement — the picture soft-gate stays uniform since a catalog/example picture is reasonable for every category. Next: decision-gated backlog (real submission backend) or the Sprint 9 v2 nicety (improvements ↔ plot legend cross-check, now sharper with categories — only structure/hardscape/pool are expected to show on the plan).
- **Post-Sprint-11 (2026-07-04)** — attachment ergonomics, committed `0dce60a`/`ddddc91`. Drag-and-drop + clipboard-paste for every file input via a new leaf module `assets/dropzone.js` (`registerDropzone`: DataTransfer + synthetic input/change events so downstream listeners run unchanged; accept-mismatch shake; appends on `multiple`; paste routes hovered → focused → single-visible zone); photo attachments render framed thumbnails with Replace/Remove (blob-URL cache revoked on replace/remove); sidenav Clear moved into a danger-zone treatment. Roadmap extended with Sprints 12–16 from the 2026-07-04 issue dump (incl. the Sprint 15 submission-architecture research verdicts: Cloudflare Worker+R2 is the zero-cost path; Cloudinary and form relays ruled out).
- **Sprint 12 (2026-07-04)** — quick fixes: numbering, redundancy, tool chrome, complete. **Work-area close-ups are area-agnostic and multi-file** (issue 14): the shot moved out of the back-yard group into a standalone `PHOTO_CLOSEUP` constant + its own photo group shown whenever any area is checked, titled "Close-up(s) of the work area(s)"; the input is `multiple` (dropzone appends, picker replaces — neighbor-form semantics), thumbnails render one card per file with per-file Remove (FileList rebuilt via DataTransfer), and the draft's `photos` value for it is a filename list (restore/preview/print/manifest all handle string-or-array; the `back_closeup` id is kept so old drafts restore unchanged). **Improvement rows renumber** on every add/remove (`renumberImprovements()` — display labels from DOM position, ids stay monotonic). **The manual sketches attestation is gone** (checkbox, `sketchesConfirmed()`, gate/packet/mailto/SUB_LABELS entries, restore loop, orphaned CSS) — Section 02 itemizes all of it now, so every packet row is derived; old drafts' `req_sketches` restores into nothing, same as `req_fee`. **Instant tooltips** cover the full draw chrome: stamp picker switched from native `title` to `data-tip` with an up-flowing tooltip, Undo/Redo likewise (Redo advertises Ctrl+Shift+Z). **`ICON.fill` is the user-supplied 48-box bucket glyph** restyled to rail conventions (stroke 3.8 ≈ neighbors' visual weight); `cursorForMode()` now reads each icon's viewBox instead of assuming 24, scaling the cursor halo/ink strokes to match. Verified in-browser (fresh port 8141, zero console errors incl. a draft-restore load): closeup group visibility, 2-file attach/count/thumbs, per-file remove, packet "1 of 4" + subrow filenames, list-shaped draft + plural is-prior restore, renumbering incl. remove-all re-add, data-tips present with no titles, 48-box fill icon. `node --check` clean; 29/29 geometry tests. Needs the user's eyes: tooltip hover-feel on the rail/stamp picker and the Fill tool's glyph cursor at real zoom. Next: Sprint 13 (eraser & object lifecycle).
- **Print-first journey rework (2026-07-05)** — user decision after debugging "Open pre-addressed email does nothing": the app-side flow was verified correct end-to-end, but the machine's `mailto:` handler (Windows new-Outlook AppX) silently no-ops even for a trivial short link fired from the OS shell — and the generated URL (~1.7 KB on a near-empty form) would exceed Windows' ~2 KB external-protocol ceiling on any real application anyway. Rather than harden an unfixable handoff, **the journey now ends at the printable packet**: one "Save & print" finish step; `buildPrintHTML()` emits three pages — submission instructions (page 1: steps, the specialist's email, derived include-list + still-outstanding warning, deadline/fee/assistance), the application, and the adjacent-owner signature form (last page, six blank wet-signature rows, change summary from the Section 02 items). Removed: the email flow (`EMAIL_TO`/`openMailto`/`startEmailFlow`/`finishEmail`, both email buttons), the separate `printNeighborForm()` window, the `#neighbor-form-file` re-attach loop, and the whole Adjacent Owners roster section (sections renumbered: 05 Acknowledgments, 06 Review & Submit; old drafts' `neighbors`/`neighborForm` keys restore harmlessly). Progress meter now counts three packet slots; the advisory review gate stays, serving the print path. `node --check` clean. Needs the user's eyes: the printed 3-page packet (instructions content, page breaks, signature-form layout) and the reworked Section 06.
- **Baked parcel extract + community-center start view (2026-07-05)** — external-dependency reduction, user-requested. New `tools/fetch-parcels.mjs` bakes `assets/parcels.json` (7,517 parcels — every polygon + situs address in the streets.json bbox, from county layer 50 PARCELS_CREST via 4 paged queries; 3.7 MB raw / ~650 KB gzipped, coords rounded to 6 decimals, APN-sorted for stable diffs). map-wizard.js is now **local-first for all parcel data**: `locateByAddress` matches number+name+city against the baked records (zero county round-trips for in-area addresses), `loadParcels` filters the extract by envelope (318 parcels / 2.4 ms for the standard 600 m box — and the baked records carry their addresses, so the follow-up assessor label query is gone too), and `fetchParcelGeometryByAPN` checks the extract before fetching; `bakedParcelFeature()` rebuilds records into the exact Feature shape the live queries return so everything downstream is source-agnostic. Live county endpoints remain as fallback (out-of-bbox addresses, extract load failure, envelopes past the baked edge). The map now opens on the **community's center of mass** ([-117.035692, 33.953955], mean of all baked parcel centroids, stored as `parcels.json`'s `center` with `HOA_CENTER` as pre-load fallback) — the old constant was downtown Beaumont, ~5 km east of the actual tracts. Remaining live-at-runtime: OpenFreeMap basemap tiles, county aerial (Orient tiles + Draw backdrop export), Overpass (out-of-bbox streets only), Google Fonts. `node --check` clean; 31/31 geometry tests. Needs the user's eyes: address lookup feel (should be instant), parcel labels/click-select from baked data, map's initial framing, and a DevTools-offline run of Select with the county blocked.
- **Print-packet overhaul planned (2026-07-05)** — new user request list turned into ROADMAP Sprints 17–22 (ahead of the still-open 14–16): (17) drop the Preview modal, drive the paged.js render from a Save/Print header; (18) IndexedDB attachment persistence via vendored idb-keyval so photos survive a reload; (19) client-side image-compression budget (25 MB target) + dedicated print pages for Improvements/Site-Plot/Photos with scaled uncropped images + multi-photo-per-improvement; (20) rename Section 06 to "Save & Print Packet" and reframe its checks from packet artifacts to workflow-step status; (21) redesign the cover (concise, review-dates in a right rail, neighbor signatures at the bottom); (22) Adobe Acrobat Sign text-tag fields on the saved PDF. Pivotal decision logged in the roadmap preamble: stay on paged.js (Path A, recommended — best-effort compression + Adobe text tags) vs. generate the PDF with pdf-lib (Path B — hard size cap + real AcroForm fields, but a large rewrite).
- **Sprint 18 (2026-07-05)** — attachments that survive a reload (IndexedDB), complete. File inputs only held bytes for the session and the localStorage draft persisted filenames only, so a reload showed a "previously attached" (`is-prior`) note with no image. Vendored **idb-keyval 6.2.2** (exact pinned ESM build, ~600 B, MIT, zero deps) into `assets/vendor/` and added a leaf wrapper `assets/attach-store.js` (`saveAttachment`/`loadAttachment`/`delAttachment`/`clearAttachments`/`clearAllAttachments`/`idbAvailable`, one `createStore("fairway-canyon-arc","attachments")`, keys `<refId>::<inputKey>` — `photo:<shotId>` · `imp:<uid>` · `plot`). app.js drives it with four helpers next to `saveDraft`: `persistAttachment()` (mirror an input's files to IDB on every commit — wired into the photo/improvement/plot `change` listeners **and** the by-hand Remove/remove-one paths that fire no change event; also `scheduleAutosave()`s so the draft carries the refId the blobs are stored under), `hydrateInput()` (push stored Files back via `DataTransfer` + a synthetic bubbling `change` so the input's existing thumbnail/status/progress listeners run), `rehydrateAttachments(d)` (async, fired at the end of `restoreDraft()` — walks `d.photos` ids, every `.improvement` row's `uid`, and the `plot` upload, upgrading `is-prior` → live wherever bytes survive), and `wipeCurrentAttachments()`. Key subtleties: improvement rows gained a **stable `uid`** (`items[].uid`, additive) because their monotonic `data-improvement` idx re-counts from 1 on restore — an idx-keyed blob would rehydrate onto the wrong row; a `rehydratingAttachments` flag stops the synthetic `change` events from immediately re-persisting what restore just read; the refId (adopted from `d.refId` at the top of restore) is the linchpin tying writes and reads together. Both draft-delete handlers `await wipeCurrentAttachments()` before `location.reload()` (a bare fire-and-forget delete gets cancelled by the reload); `?reset` purges the whole store. Everything degrades gracefully (`idbAvailable()` + try/catch) to today's filename-only `is-prior` path if IDB is unavailable or a write throws. Verified in-browser (fresh port 8155, programmatic File round-trip): persist-on-attach lands both a photo and an improvement picture in IDB under the namespaced keys; **reload rehydrates both as live Files** (`is-attached` + rendered thumbnail, not `is-prior`); a 2-file close-up rehydrates 2 live files + 2 thumb cards; single-Remove deletes the IDB key; `clearAttachments(refId)` empties the store; zero console errors throughout. `node --check` clean on app.js/attach-store.js/idb-keyval; 37/37 geometry tests. Needs the user's eyes: a real OS-picker attach + reload, and a private-mode/IDB-blocked run (should fall back silently). Next: Sprint 19 (compression budget + dedicated print pages for improvements/plot/photos with scaled images).
- **Sprint 19 (2026-07-05)** — compression budget + dedicated print pages, complete. The print packet crammed the whole application onto one page and carried **no photos at all** (page 2 had only a "N photos attached" text note). Now the heavy sections each get their own print page with real, scaled, uncropped images, and a best-effort compression pass keeps the packet emailable. **Image budget:** new pure module `assets/image-budget.js` — `allocateImageBudget(sizes, total)` water-fills a byte target across images (per-image floor `min(size,60KB)` + original-size cap, surplus from capped images redistributed to those with headroom; if floors alone bust the budget, squeeze proportionally to floors), `dataUrlByteLength`, and the `ENCODE_LADDER` (maxDim/quality steps) — 10 `node --test` cases (`tests/image-budget.test.js`, 47 total). The DOM encode lives in app.js: `preparePrintImages(d)` gathers requested photos (questionnaire order) + each improvement's pictures + the plot (rendered PNG in build mode, uploaded image in upload mode), allocates the 24 MB budget, and `encodeToBudget` walks the ladder (canvas downscale + JPEG re-encode, alpha flattened to white) until each image fits its target; the achieved compressed total is surfaced in the status line. **Multi-photo per improvement (item 2):** the row's picture input is `multiple`; `improvementItems().photo` is a list (older drafts' single string normalized by `itemPhotoNames`); thumbnails render one card per file (shared `thumbCardHTML`, non-image files get a file-type placeholder) with per-file Remove via a delegated `[data-imp-remove-one]` handler mirroring the photo close-up path. **Async `printPreview`:** opens the popup synchronously (gesture-tied so the blocker allows it) with a "preparing…" placeholder, awaits compression, then `document.open()`+writes the real paginated doc. **New pages** (`buildApplicationPageHTML` trimmed to applicant+acks+signature; `buildImprovementsPageHTML` — one `break-inside:avoid` block per item with a row of its scaled pics; `buildPlotPageHTML` — plot image + legend on its own page, omitted when nothing drawn/uploaded so no empty page; `buildPhotosPageHTML` — one full-width uncropped image per row, captioned). Every print `<img>` carries `aspect-ratio` from its encoded pixel dims so paged.js measures without decoding. **Removed** (dead after the restructure): `improvementsTableHTML`, `SUB_LABELS`, and the `.print-columns`/`.print-col`/`.print-improvements`/`.print-plot`/`.print-checklist` CSS. Verified in-browser (fresh port 8177, stubbed `window.open` capturing the doc): multi-file attach → 2 thumb cards + "Attached: 2 pictures", per-file Remove leaves 1; the async flow compresses and writes instructions + trimmed application + Proposed Improvements (2 compressed catalog JPEGs) + Property Photos (1 compressed JPEG) + neighbor form; plot page omitted with nothing drawn, renders the uploaded image in upload mode; old table gone; zero console errors. `node --check` clean on app.js/image-budget.js; 47/47 tests. Needs the user's eyes: the real print popup — paged.js pagination of the new pages, image legibility/scaling, and the achieved PDF size after "Save as PDF". Next: Sprint 20 (rename Section 06 to "Save & Print Packet", reframe its checks from packet artifacts to workflow-step status).
- **Path A locked + Sprint 17 (2026-07-05)** — user locked **Path A** (stay on paged.js; Path B/pdf-lib kept only as a documented escalation). **Sprint 17 complete:** dropped the "Preview first" modal — it looked nothing like the paged.js print. Deleted `#preview-modal` + `buildPreview()`/`photoPreviewHTML()`/`ownerSigHTML`/`sigImg`/`yn`, the `#do-print` button, the "Preview first" `type=submit` button, the form-submit→preview handler (now a bare `preventDefault`), and the `openModal`/`closeModal`/preview-Escape wiring; `SUB_LABELS`/`improvementsTableHTML`/`plotLegendHTML` stayed (print-only callers). `printPreview()` now injects a `@media screen`-only `.print-bar` toolbar into the render popup (Save as PDF / Print / Close), hidden in `@media print`; the auto-`window.print()` is gone — buttons drive it and enable once `PagedConfig.after` fires (5s timer + `<script onerror>` fallback enables them if paged.js can't load, so the native `@page` flow still prints). CLAUDE.md Output-paths / Guided-finish / plot-surface passages updated. Left behind (deferred): dead preview-only CSS in styles.css. `node --check` clean; 31/31 geometry tests. Needs the user's eyes: the popup toolbar (button enable-after-pagination, Save-as-PDF destination, Close) — the print popup isn't reliably drivable from this agent's browser automation.
- **Sprint 20 (2026-07-05)** — "Save & Print Packet": step-status checks, not packet checks, complete. Section 06 audited packet *artifacts* (plot present, each photo attached, each catalog picture) — which duplicated the new dedicated print pages (Sprint 19) and read as nagging. **Renamed** Section 06 "Review & Submit" → **"Save & Print Packet"** across the card `<h2>`/`card__sub`, the sidenav link, and the masthead demo-banner. **Replaced the "Your packet" artifact list** (`renderPacket()` gone, plus `packetItemNode()`'s now-unused `subs` branch and the `.packet-sub*` CSS) **with a workflow-step overview**: new `stepStatus()` reports one row per section (01 Applicant · 02 Proposed Improvements · 03 Site/Plot Plan · 04 Photos · 05 Acknowledgments), each done/incomplete from **that section's own inputs** — a shared `checkRequired(el)` predicate (extracted from the old `fieldIssues()` body so both agree) + `sectionRequiredComplete(id)` over the section's `[required]` fields, plus the non-field signals (plot provided → 03, all requested photos attached → 04, `ownerSignatureProvided()` → 05). Renders via `renderStepStatus()` into `#step-status` (reuses `packetItemNode()`/`.packet-item`, ✓/✗ + one-line reason + "Open section" jump). **The advisory gate** (`reviewThen`) now lists incomplete **steps** (jump-links to each `<section>`), still never blocking, still paints inline `[required]` errors via `fieldIssues()` first so arriving at a step shows its gaps. **Catalog pictures no longer gate anywhere** — dropped from the meter and the gate/print notes (`improvementChecklist()` survives only to list *attached* pics in the print include list); the meter re-derives from steps (required fields + signature + two step signals: plot, photos — 100% still unreachable empty). The page-1 print "still to finish" note is now `stepStatus()`-derived (`packetMissingList()`/`allIssues()`/`packetTargetFor()` removed). Verified in-browser (fresh port 8203): zero console errors on load + through the flow; overview renders the 5 step rows correctly; filling Applicant + one improvement name + acks + typed signature flips 01/02/05 to ✓ (03/04 stay ✗ with reasons), meter 88%; the finish gate lists only the two incomplete steps as jump buttons; the printed cover warning reads "Sections still to finish … 03 Site / Plot Plan — … 04 Photos — …"; a save→reload draft round-trip restores fields and recomputes the overview identically. `node --check` clean on app.js; 47/47 tests. Needs the user's eyes: the overview's visual read in the real card, and a fully-complete run reaching 100% once a plot + all photos are attached. Next: Sprint 21 (cover redesign — concise, review dates in a right rail, neighbor signatures at the bottom).
- **Sprint 21 (2026-07-05)** — cover redesign + incompleteness warning moved to a front page, complete. Done with a **user modification**: the "sections still to finish" note that Sprint 20 left buried inline on the instructions page moves to its own **leading "cover-cover" page** that prints *only* when something is incomplete and disappears entirely for a completed application. **`buildWarningCoverHTML()`** (new, prepended in `buildPrintHTML`) returns `""` when `stepStatus().filter(!ok)` is empty, else renders a full stop-page: diagonal hazard bands (top + bottom, `repeating-linear-gradient`, print-color-adjust exact), an inline-SVG maroon warning triangle with a white "!", a "Stop — do not submit yet" eyebrow + Georgia "This application is incomplete" title, a maroon-headed **"Still to finish" card** (one row per incomplete step: num + label + reason), and an **"You must attach any outstanding photos or files to your submission email"** callout. **Cover reworked** (`buildInstructionsHTML`): two-column grid (`.print-cover`) — trimmed steps + "What to email" include-list + deadlines/fees on the left, a cream **right rail** (`.print-rail`) holding the 2026 review dates + a Questions? contact card. **`printDatesHTML()` → one narrow table** (was two side-by-side columns): abbreviated months (`January 29` → `Jan 29`) and `(tentative)` collapsed to a "† tentative" footnote so the rail column stays tight. **Neighbor signatures folded onto the cover** (user chose fold-and-retire over keep-both): `buildNeighborFormHTML` (standalone last page, 6 rows) retired; new **`buildNeighborStripHTML(d)`** + **`neighborChangeSummary(d)`** render a compact 4-row signature strip pinned to the **bottom** of the cover via a flex column (`.print-cover-page { display:flex; flex-direction:column; min-height:8.8in }` + `margin-top:auto` on the strip), with a maroon divider reading as a "detach here to sign" rule. CSS: added `.print-warncover*`, `.print-cover`/`.print-cover-page`, `.print-rail*`, `.print-neighbors*`; removed the dead `.print-warn`/`.print-dates`(flex two-col)/standalone `.nf-doc|header|eyebrow|title|contact|info|label|proposal|footer` rules (shared `.nf-section|note|table|num|sig|date` kept + restyled for the strip). Verified visually in a standalone HTTP-served harness driven by Chrome (both pages screenshotted at letter proportions): the warning page fills a sheet with hazard bands top/bottom, the triangle + card + attach callout read cleanly; the cover is concise and balanced with the dates rail, and the signature strip pins to the page bottom under its divider. `node --check` clean on app.js; 47/47 tests (37 geometry + 10 image-budget). Needs the user's eyes: the real paged.js print popup — the warning page's page-break behavior with 1–5 incomplete steps, and the cover's flex-column min-height under real paged.js pagination (a completed packet should open straight on the cover with no warning page).
- **Sprint 21 follow-up (2026-07-05)** — two user tweaks after eyeballing the first cut. (1) **No solid fills on the warning page** — converted every solid ink area to line-art: the hazard bands became a **diagonal caution *hatch*** (thin maroon diagonals on white via `repeating-linear-gradient(... 2.5px, transparent ...)` + hairline frame) instead of solid alternating stripes; the warning triangle is now an **outlined** stroke path (maroon stroke, `fill:none`) with a stroke-drawn "!" (rounded-cap line bar + a zero-length round-cap line as the dot) rather than a solid maroon triangle; the "Still to finish" **card header** dropped its solid maroon background for maroon text + a bottom border; the attach callout dropped its `#faf3f2` fill for a full 1px border + the maroon left accent. (2) **Cover uses the spare whitespace** — bumped the left-column instructions type up ~1.5px (steps 11→12.5, include/info 10.5→12, email 13→14, cover `h4`→13) and grew the folded signature strip from **4 to 6 rows**; the flex-column `margin-top:auto` still pins it to the page bottom. Re-verified both pages in the standalone Chrome harness: the warning page reads as clean outline/hatch line-art with no heavy ink, and the cover is better-balanced with the larger type + 6 rows. Docs synced (CLAUDE.md/ROADMAP.md). `node --check` clean; 47/47 tests.
- **Sprint 22 (2026-07-05)** — Adobe Acrobat Sign text tags on the saved PDF (item 7), complete (code); the HOA-usage decision stays open. Under Path A the realistic e-signature path is **Adobe Sign text tags** — literal strings the PDF carries that Adobe Sign auto-detects **on upload** and converts to fillable fields (no library, no Path-B rewrite). Added a `esignTag(directive)` helper that emits one near-invisible `.print-esign-tag` span (5px, near-white `#efece7`) or `""` when tagging is off, and an `ADOBE_SIGN = { enabled:false, neighbors:true }` config near the print builders. Owner signature/date on the application page tag as `signer1` (`Sig`/`Dte`); each of the 6 adjacent-owner rows on the cover strip becomes its own participant (`signer2…signer7`) with fullname/address/signature/date tags, gated additionally on `ADOBE_SIGN.neighbors` (false → owner-only e-sign flow). **Default OFF**, so a normal packet is byte-for-byte unchanged (the helper returns `""`); flip `enabled` on only if FSResidential routes packets through Adobe Acrobat Sign. **Left open (HOA decision):** does FSResidential use Adobe Sign, and owner-only vs. each-neighbor-a-signer — the flag ships off until confirmed; if plain-PDF AcroForm fields are required instead, that's the documented Path-B/pdf-lib escalation, not built. Verified: a throwaway node harness proves the exact directive strings + all toggle states (OFF → empty; ON → owner tags exact + 6 distinct neighbor signer roles; `neighbors:false` → owner-only); browser smoke test (fresh port 8231) shows zero console errors on load and the app booting with all 7 sections on the unchanged default-OFF path. `node --check` clean on app.js; 47/47 tests (37 geometry + 10 image-budget). Needs the user's eyes / an HOA answer: the real paged.js print popup with `enabled:true` (tags invisible on paper; Adobe field detection + sizing on upload), and whether to enable it at all. This closes the print-packet overhaul (Sprints 17–22); still-open backlog is Sprints 14 (callout anatomy), 15 (submission-backend decision), 16 (cross-checks / per-category enforcement).
- **Sprint 22 follow-up (2026-07-05)** — user flipped the tags **ON** (`ADOBE_SIGN.enabled = true`); they ship enabled. The factual HOA question (does FSResidential use Adobe Sign?) is still worth confirming, but the tags are harmless if unused (near-invisible, inert until upload), and `enabled:false` reverts the packet byte-for-byte. Verified the enabled path against the **real** generated print doc via a stubbed-`window.open` in-browser capture (fresh port 8232): the full paginated doc (~504 KB) embeds 26 tag spans — owner signer1 sig+date + 6 neighbor rows × 4 fields (fullname/address/signature/date) — all 6 neighbor signer roles distinct (signer2–7), zero console errors. `node --check` clean; 47/47 tests. Needs the user's eyes: the real paged.js print popup (invisible on paper) + an actual Adobe Sign upload (field detection/sizing).
- **Roadmap reconcile (2026-07-05)** — per the user, marked **Sprint 14 done (ad hoc)** and **shelved Sprints 15 & 16**. Sprint 14 (callout label/leader independent repositioning, item 6) was never a standalone session — it landed inside Sprint 13's edit-anchor work: a selected callout grows two `buildEditAnchors`/`anchorSpecsFor` handles (arrowhead handle re-aims the leader tip; label-box handle moves the text with the leader base riding along, both box-clamped), and persistence/undo/print come free through the normal `toObject`/`Konva.Node.create` + `attachShapeInteractions` rehydrate path — verified present in plot-editor.js (`anchorSpecsFor` callout branch, ~L1359). The stretch (stamp resize-on-select) was not pursued. **Sprint 15** (submission backend decision) shelved: the app stays a zero-transmission print-first mockup; the Worker+R2 research is retained as the escalation path if the HOA ever wants in-app submission. **Sprint 16** (improvements↔plot cross-check + per-category required-field enforcement) shelved as advisory niceties. Docs synced: ROADMAP.md (14 ✅, 15/16 🗄️ SHELVED), CLAUDE.md roadmap line. No code change.
- **Masthead place cue (2026-07-05, ad hoc)** — user asked to put the community clubhouse in the header "so people know exactly where they are." Added a watercolor-rendering hero band below the masthead title (`.masthead__scene` in index.html) with a crimson-ticked location plaque (`.masthead__place`: "You're applying to · **Fairway Canyon** · Community Clubhouse · Beaumont, California"). Design: `mix-blend-mode: multiply` on the `<img>` melts the painting's white margin into the warm paper ground (reads as painted onto the letterhead, not a boxed photo); a vertical `mask-image` gradient feathers the cover-crop's top/bottom cuts into the page; a bottom grounding wash keeps the plaque legible. Responsive `clamp()` band height + fluid type; `display:none` in `@media print` (the on-screen page isn't the packet). Asset pipeline: the 3.1 MB source PNG (1672×941) re-encoded via **ffmpeg** to `assets/img/clubhouse.webp` (1600w, q82, 236 KB, primary) + `clubhouse.jpg` (1400w, q3, 324 KB, `<picture>` fallback) — new folder `assets/img/`, the app's only raster brand asset; WebP is safe since the app already requires a modern browser. Verified in-browser (fresh port 8123): the band renders the building crisply at real size (looks faint only in a downscaled full-page thumbnail — confirmed clear by zooming the actual pixels), plaque legible, no console errors. Doc hygiene: added a CLAUDE.md Gotcha (don't "fix" the intentional multiply blend; the folder + fallback rationale). No sprint — a one-off visual request outside the roadmap.
- **Demo mode (2026-07-05/06, ad hoc)** — user asked for a one-click "demo mode" toggle to populate the whole form for demos/screenshots, picking a random real HOA-area house. Added a toggle switch to the "Demo / mockup" banner (`#demo-mode-toggle`) and a new standalone module `assets/demo-mode.js` (loaded by its own `<script>` tag, not part of the real app flow — see its CLAUDE.md bullet). It drives the real form like a user would: picks a random single-polygon, addressed, Beaumont parcel from `assets/parcels.json` for the applicant address; fills two sample improvements (Alumawood patio cover / drought-tolerant landscaping); drives the site-plan wizard straight to a **confirmed** plot via the same `restoreParcelFromDraft` + (newly-exported) `showStep(4)` pair `restoreDraft()` already uses for a saved parcel, then paints two illustrative rectangles via `restorePlot()` (not polygon-aware — just grid percentages, "good enough for an illustrative demo plan, not a precise one"); answers the photo questionnaire and attaches a sample photo per requested shot; types the owner signature; checks every acknowledgment. **Image sourcing:** confirmed by hand via curl that `picsum.photos` and plain `unsplash.com` page URLs do **not** send `Access-Control-Allow-Origin`, so a `fetch()` attach would fail CORS; `images.unsplash.com/photo-<id>` **does** send it. A subagent found + curl-verified 14 real Unsplash photo IDs (2 each across 7 themes: house-front, backyard, side-yard, house-wall, patio-cover, landscaping, paint-swatch); a canvas-drawn placeholder is the fallback whenever a theme has no match or a fetch fails (offline/CDN hiccup), so the feature still works with no network. Toggling off reuses `#clear-draft`'s existing confirm+wipe+reload flow rather than duplicating it. Verified in-browser (fresh port 8241): a full fill reaches 100% complete with real attached photos (incl. the plot render showing the two painted rectangles inside the real parcel outline over the live aerial), a re-toggle picks a fresh random parcel, and the print packet's async image-compression pipeline completes with no console errors (the print popup itself isn't reliably drivable by browser automation — matches the existing CLAUDE.md caveat — but its title updated from "Preparing…" to the real doc title with zero errors in the opener's console, which is what those code paths would throw into). `node --check` clean on all touched/added files; 47/47 tests (unaffected). No sprint — a one-off feature request outside the roadmap. *(Superseded by Sprint 23 below: the random-parcel picker was replaced with a fixed, verified in-HOA address after this entry was written.)*
- **Sprint 23, part 1 (2026-07-05) — Phase 1 investigated and ruled out, not implemented.** Before touching `geometry.js` per the roadmap's planned "unify the two projections" fix, ran an empirical check first: a faithful line-for-line replication of both `buildParcelGrid()` (geometry.js) and `fetchAerialBackdrop()`'s exact pixel math (map-wizard.js), fed real ring coordinates from `assets/parcels.json` (11506 Aaron Ave — the user's trusted reference — plus two other real, larger/irregular parcels) into the app's actual `fitSimilarity()` call, at bearings 0°–200°. Result: residual after the best-fit similarity is **~1e-4 ft at every parcel/bearing tested**, not the several feet of visible drift the sprint was written to fix — `fitSimilarity`'s scale term already absorbs the whole equirectangular-vs-Web-Mercator gap as a pure uniform scale (fitted scale ≈ 1/cos(latitude), confirmed both analytically and numerically), since Mercator's distortion is isotropic at parcel scale. Reprojecting the grid onto Web Mercator would have been a no-op on the visible bug. Presented this finding to the user (with the math) **before** writing any code; user chose to skip Phase 1 and proceed straight to Phase 2. No `geometry.js` change from this finding, no regression risk.
- **Sprint 23, part 2 (2026-07-05) — Phase 2 (ground-truth calibration) + demo-mode address fix.** Replaced the flat `AERIAL_NUDGE_FT` constant (plot-editor.js) with `AERIAL_CONTROL_POINTS`, a list of hand-calibrated `{lng, lat, east, north}` points inverse-distance-weighted per parcel by a new pure function `interpolateAerialNudge(lng, lat, points)` (geometry.js — 5 new `node --test` cases covering 0/1/2+ points, exact-match, equidistant-average, and closer-point-dominance). Seeded with exactly **one** point (11506 Aaron Ave, carrying forward the old flat constant's exact value `east: -1.0, north: -3.6`); verified numerically this reproduces the prior flat behavior byte-for-byte everywhere (1-point IDW is uniform by construction), so on-screen alignment is unchanged until more points are added. Built a **dev-only calibration overlay** (`?calibrate-aerial` in the URL, zero DOM/listener footprint otherwise) — arrow keys or on-screen N/S/E/W buttons nudge the aerial backdrop in 0.1 ft steps (Shift = 1 ft) via a lightweight redraw that reuses the already-decoded cached image; a live panel shows the parcel centroid, interpolated baseline, manual dial, and total, with a "Copy point" button producing the exact object literal to paste into `AERIAL_CONTROL_POINTS`. Root cause re-diagnosed: the pre-existing `AERIAL_NUDGE_FT` comment already had it right — this is the 2020 orthophoto's own ortho-rectification error against the county's cadastral vectors (genuine source-data mismatch, not a coordinate bug), which is why it varies by location and why control-point interpolation (not a formula fix) is the right tool. Also fixed `demo-mode.js`'s `DEMO_PARCEL`: swapped "36692 Gallery Ln" (confirmed Tournament Hills, a different Beaumont community) for 11506 Aaron Ave/APN 413882015 (the same trusted reference), ring cross-referenced from `assets/parcels.json` by APN. Doc cleanup: CLAUDE.md's aerial-alignment gotcha rewritten (not appended) to describe the new scheme; `geometry.js` module bullet and demo-mode.js bullet updated (the latter also corrected a stale description of random-parcel selection the code no longer does); test count updated to 52. `node --check` clean on all touched files; 52/52 tests. **Needs the user's eyes** (this agent's browser automation can't reliably drive the Draw step's live map/Konva surface — see CLAUDE.md): confirm Aaron Ave looks unchanged, then try the calibration overlay on a few more real Fairway Canyon lots (Champions Dr, Palmer Ave, Faldo St, Trevino Trl, Ouimet Way) to gather more control points. The final Sprint 23 item — baking in a real hand-drawn demo site plan — stays deferred until that calibration pass is done.
- **Sprint 23, part 3 (2026-07-05) — first calibration round + a proper address-finding tool.** User gathered 5 points with the overlay in one pass; before adding them, cross-checked each against a Fairway Canyon boundary derived from evidence (no HOA-boundary field exists in `parcels.json`): took the bbox of parcels on the 6 previously-confirmed streets, then sanity-checked it by listing every street name inside — the result is ~80 streets, nearly all PGA/LPGA golf-legend surnames (Hogan, Nicklaus Nook, Sorenstam, Couples, Snead, Zoeller…) plus "Tukwet Canyon" itself (the community's own golf club), a strong self-consistent signal, and none of the previously-ruled-out streets (Gallery Ln, Brutus Way, Crenshaw St) appear in it. Also caught the bbox leaking ~41 Calimesa parcels at its north edge (a different city — found by inspecting one flagged address's `city` field rather than assuming) and added a `city` filter. Result: **3 of the 5 gathered points landed inside** (34928 Roberts Pl, 11455 Aaron Ave, 35339 Stewart St) and were added to `AERIAL_CONTROL_POINTS`; **2 landed well outside** (Brutus Way, Crenshaw St — both technically `city: BEAUMONT` but ~1.6–2.1 mi from the community center, almost certainly a different Beaumont subdivision) and were deliberately left out. Verified numerically: exact control points round-trip unchanged through `interpolateAerialNudge`, other locations now get smoothly-blended non-uniform corrections. Built **`tools/find-calibration-addresses.mjs`** (new dev-time script) to make future rounds systematic rather than ad hoc: prints the 4 compass extremes plus a greedy farthest-point-sampled interior spread from the same evidence-derived bbox. `node --check` clean on all touched files (incl. the new tool); 52/52 tests. **Needs the user's eyes:** run the new tool, visit its recommended addresses with `?calibrate-aerial`, and report back more points.
- **Sprint 23, part 4 (2026-07-05) — second calibration round.** User ran `tools/find-calibration-addresses.mjs` and visited its recommended addresses. Some were still bare dirt in the 2020 imagery (undeveloped at capture time — a real limitation of that imagery's vintage, not a bug in the tool or the app) but 3 usable points came back: 11284 Demaret Dr, 11010 Coody Ct, 11429 Lyle Ln — all added to `AERIAL_CONTROL_POINTS` (7 points total). User flagged that Coody Ct's fit looked visibly worse than the others; its magnitude (~8 ft) is indeed 3–4× every other point. Checked numerically before treating it as routine: with only 7 points, it measurably shifts the interpolated correction even at the far side of the community (~1 ft difference at Champion Dr, nearly a mile away, with vs. without it) — not a purely local effect. Presented the numbers and asked the user how to handle it (keep / re-calibrate / drop); **user chose to keep it as reported**, on the reasoning that spatially-varying ortho error is exactly what this scheme exists to capture. No code change — logged here as the record of that decision in case the point ever looks wrong on screen and needs revisiting. `node --check`/`node --test` clean (52/52) after adding the 3 points.
- **Sprint 23, part 5 (2026-07-05) — third calibration round, 11 points total.** User gathered 4 more before checking any prior round's on-screen result: 35216/35315/35290 Hogan Dr and 11014 Watson Way (all confirmed Beaumont, inside the evidence-derived bbox, near-exact parcel-centroid matches). Added without re-litigating magnitude this round — none was the kind of outsized (3–4×) outlier Coody Ct was, and the user already established that ordinary point-to-point variance is expected, not a red flag to second-guess by default. `AERIAL_CONTROL_POINTS` is now 11 points, spread across most of the community. `node --check`/`node --test` clean (52/52). **User has not yet looked at any round's result in the browser** — worth doing a visual pass now that coverage is reasonably dense.
- **Sprint 23, part 6 (2026-07-05) — rapid multi-house calibration walk.** User reported the one-house-at-a-time workflow (re-run the whole wizard per address) was too slow given they suspect real localized variation, and asked for Shift+Right/Left house-to-house navigation, keyboard nudging (already had it), and an "Update"/refresh button. Extended the `?calibrate-aerial` overlay (plot-editor.js) rather than building a separate tool: `loadCalibCandidates()` lazily fetches `parcels.json` and builds a ~150-stop serpentine-ordered (row-by-row, alternating direction, so consecutive stops stay geographically close) walk over the same evidence-derived Fairway-Canyon-bbox-plus-Beaumont-city filter `tools/find-calibration-addresses.mjs` uses; **Shift+→/←** auto-commits the current house (only if actually nudged — a no-op skip for already-aligned or bare-dirt lots) into a running session log keyed by APN, then jumps via `restoreParcelFromDraft`+`showStep(4)` (the same pair demo-mode.js already uses) reached through a **dynamic** `import("./map-wizard.js")` specifically to avoid adding a *static* plot-editor.js→map-wizard.js edge to the module graph (map-wizard.js already imports FROM plot-editor.js — a static back-import would form a new cross-module cycle affecting every page load, not just this dev-only flag). Repurposed keyboard Shift+Left/Right away from "1 ft big-step nudge" (now navigation) — Shift+Up/Down keeps that role, and the on-screen buttons still support Shift-click for a big step in any direction. Added an "Update" button (re-fetch/redraw the current house without advancing, committing first) and a growing session-log textarea with one "Copy all" button instead of per-stop copying. `node --check` clean; 52/52 tests (no logic in geometry.js changed, so no new test cases needed). **Not verified in-browser** (this agent's browser automation can't reliably drive the Draw step — see CLAUDE.md's verification-limits note) — needs the user's hands-on test of the new keybindings/buttons before trusting the workflow.
- **Sprint 23, part 6 follow-up (2026-07-05)** — user asked for Ctrl+arrow instead of Shift+arrow for house navigation before even testing the first version. Swapped: **Ctrl/Cmd+→/←** now walks houses (unchanged action — commit-if-nudged, then jump), and **Shift** is uniformly "1 ft step" on all four nudge directions again (fixes the asymmetry the first version had, where Shift meant a big nudge step on Up/Down but navigation on Left/Right). `node --check` clean.
- **Sprint 23, part 7 (2026-07-06) — the rapid walk tool proves out, 26 points total.** User ran the new Ctrl+arrow walk and returned 15 points in the tool's own session-log format (`{ lng, lat, east, north }, // address`), confirming the feature works end-to-end as designed. All 15 checked out clean on inspection (in-bbox, `city: BEAUMONT`, exact address match to the nearest parcel, and — per the Coody Ct precedent — no outsized (3–4×) magnitude outlier among them; range was a consistent ~1.1–5.2 ft) — added directly to `AERIAL_CONTROL_POINTS` with no exclusions needed this round, unlike the very first manual round. Streets covered: Smith Ave (×4), Michelle Ln (×2), Goalby Dr, Thorpe Tr, Trevor Way, Stockton St, Roberts Pl, Aaron Ave, Dylan Ct, Byron Tr. `AERIAL_CONTROL_POINTS` is now **26 points** with dense coverage across most of the community's interior, not just its edges. `node --check`/`node --test` clean (52/52). **Still not verified visually in the browser** — that's the natural next step now that density is much higher.
- **Sprint 23, part 8 (2026-07-06) — fifth calibration round, 34 points total, one dedup caught.** User returned 10 more points from the same walk tool. All 10 checked out clean (in-bbox, `city: BEAUMONT`, exact address match, magnitude ~1.1–5.5 ft, no outlier). But 2 of the 10 — 35368 Smith Ave and 35942 Dylan Ct — turned out to be **re-visits of houses already in the fourth-round list**, at the exact same `{lng, lat}`, each with a noticeably different manual reading (Smith Ave: 5.18 ft old vs 4.36 ft new; Dylan Ct: 1.11 ft old vs 1.35 ft new, different direction). Caught this before adding, because leaving both as literal duplicate entries would have created a real inconsistency in `interpolateAerialNudge()`: its exact-coordinate-match branch returns early on whichever entry iterates first, so re-drawing *that* house would silently ignore the second reading entirely — while every *other* house in the neighborhood would see that location double-weighted in the IDW blend (two entries at identical distance instead of one), since ordinary interpolation math doesn't special-case duplicate coordinates. Fixed by averaging each pair into its single existing fourth-round entry (Smith Ave → `-4.15,-2.34`; Dylan Ct → `0.37,1.05`) instead of appending the second reading as a new point. The other 8 clean points (Stewart St, Michelle Ln, Lyle Ln, Locke Ln, Stockton St, Anderson St, Trevino Tr ×2) were appended as a labeled fifth round. Net: 26 → 34 total entries (8 net new + 2 merged, not 10 appended). `node --check`/`node --test` clean (52/52). **Still not verified visually in the browser.**
- **Sprint 23, part 9 (2026-07-06) — sixth calibration round, 44 points total.** User returned 11 raw points from the same walk tool as a pasted session log with no address comments (the tool's "Copy all" output is bare `{lng, lat, east, north}` literals). Resolved each to a real address by point-in-polygon lookup against `assets/parcels.json` (same technique as `tools/find-calibration-addresses.mjs`'s evidence bbox, applied per-point instead of per-bbox): all 10 unique coordinates landed on real, addressed, in-bbox Beaumont parcels (Anderson St ×2, Hogan Dr ×3, Miller Pl ×2, Casper Cove, Rosburg Rd, Coody Ct). One pair of the 11 raw entries was an **exact duplicate within the same paste** (identical `{lng, lat, east, north}` for 11286 Coody Ct, not a re-visit of an earlier round) — collapsed to a single entry per the same reasoning as the part-8 dedup. Flagged one point worth watching: **11286 Coody Ct** shows a ~9 ft correction, in the same range as the existing 11010 Coody Ct outlier from part 4 — two different houses on the same street both landing far outside the ~1–4 ft norm suggests a localized orthophoto problem specific to that block, not a bad manual reading; kept as reported per the standing precedent (part 4) of trusting the user's on-the-ground read over second-guessing magnitude. `AERIAL_CONTROL_POINTS` is now **44 points**. `node --check` clean on plot-editor.js. **Still not verified visually in the browser** — this agent resolved addresses and merged the data but did not drive the map/Konva surface (per CLAUDE.md's verification-limits note).
- **Sprint 23, part 10 (2026-07-06) — Align Corners: manual per-vertex boundary correction, a real applicant-facing feature.** After the six calibration rounds above, the user reported alignment was *worse* in places and suspected the real problem wasn't a whole-photo translation error at all: some county parcel vertices may be locally wrong (fence/pavement doesn't match the platted lot line), and no uniform nudge can fix a genuine per-vertex shape mismatch. Rather than gathering more control points, the user asked for a new tool that lets the applicant directly drag the boundary's own corners to match their own photo. Two decisions confirmed before implementation: this ships as a **real production feature** (every applicant, not a dev aid), and it lives as a **new tool mode inside the existing Draw step** (not a new wizard step), since the aerial photo and boundary trace are already both rendered there. Planned via `EnterPlanMode` with two Explore agents (mapping the existing `buildEditAnchors`/`anchorSpecsFor` reshape-handle pattern and the full aerial-registration/persistence pipeline) and a Plan agent, then implemented: a new pure helper `applyPolygonOffsets(polygon, offsets)` (geometry.js, 4 new `node --test` cases) layers a sparse `boundaryAdjust` correction (plot-editor.js) on top of the pristine `parcelPolygonPx` via one combining helper, `adjustedParcelPolygon()`. Every boundary-tracing/containment consumer switched to it: `clipToParcel` (fixes both the live paint clip and print, since it's reused verbatim by both), `insideParcel` (Stamp containment), and a new shared `buildParcelOutlineNode()` factory (replacing duplicated inline `Konva.Line` construction in `rebuildBgLayer` and `renderPlotImage`). Critically, `buildAerialNode`'s `fitSimilarity` call and `centerOnParcel`'s bbox were **left untouched** — they must keep reading the pristine polygon, or a corner drag would re-warp the whole aerial photo's rotation/scale/translation as a side effect. A new `redrawParcelOutline()` (modeled on the existing dev calibration overlay's `reapplyAerialNudge()`) gives drag-tick-cheap redraws without re-decoding the aerial image. Discovered mid-plan and confirmed against the actual code: county rings repeat their first vertex as their last, so a closed N-vertex ring has only N-1 real corners (`distinctCornerCount()`) — dragging vertex 0 mirrors its offset onto the closing duplicate so the ring never shows a seam. New tool: **Align Corners** (hotkey `A`, in `TOOL_MODES` between Select and Pan), with a "Reset corners" button in a new `#align-control` contextual status-strip slot (same pattern as the brush/stamp controls). Wired into the Done-lock (`setPlotConfirmed` destroys/rebuilds handles, since Konva drags don't consult stage-level guards) and undo/redo (`snapshotState`/`applyHistorySnapshot` now capture `boundaryAdjust` too, alongside `cellState`/`drawLayer` — a third parallel-persistence path per the project's own documented "forget one and it silently drops out" gotcha). Persisted as an additive `plot.boundaryAdjust` field (`serializePlot`/`restorePlot`), no `PLOT_VERSION` bump. Every line number in the plan was verified against the actual working tree (not assumed from the sub-agents' reports) before implementing. `node --check` clean on all touched files; 61/61 tests (51 geometry + 10 image-budget, up from 57 — 4 new `applyPolygonOffsets` cases). **Needs the user's eyes** — this agent cannot reliably drive the Draw step's live Konva surface: confirm dragging a corner moves only the boundary line (not the photo), Stamp placement/print respect the correction, Reset/undo/reload all work, and the lock/unlock lifecycle behaves.
- **Landing gate reintegration tried and reverted; masthead watercolor scoped to the landing page only (2026-07-06, ad hoc).** User asked to fold the landing gate's content into the form as a permanent first section and delete the gate. Built and verified working (full-screen `#landing` removed, its content — checklist, dates, how-to-submit/assistance/fees — merged into a new `01 Requirements & Dates` form section, `enterForm()`/`showLanding()` and the whole hidden-attribute gating mechanism deleted, everything renumbered and re-tested clean), but **the user's wife didn't like it** — reverted via `git checkout` back to the prior commit (nothing had been committed yet) to restore the separate landing gate with `01 Applicant Information` as the form's first section, exactly as before. Decision for future sessions: **keep the landing gate as a distinct pre-form screen — don't re-attempt merging it into the form.** Separately (same request), scoped the masthead clubhouse watercolor (`.masthead__scene`) to the landing page only: `body:has(#form-layout:not([hidden])) .masthead__scene { display: none; }` (styles.css) hides it once inside the form, reusing the existing `enterForm()`/`showLanding()` hidden-attribute toggle — no JS changes needed, pure CSS. Verified in-browser (fresh port, `localStorage.clear()`): watercolor visible on the landing screen, hidden after "Start application", reappears via the "← Requirements & dates" link; `01 Applicant Information` confirmed as the form's first card; no console errors; `node --test` clean (61/61, unaffected — CSS-only change).
- **Measurement halo (2026-07-06, ad hoc)** — dimension arrows/labels got a thin white outer stroke so they read over the aerial and painted materials (user request). Every measure arrow is now a pair built by `measureArrowPair()` (plot-editor.js): a white halo `Konva.Arrow` (strokeWidth 4.5, white fill so the arrowheads halo too) under the blue ink arrow — the same halo-under-stroke treatment as the hardware stamps — and the feet label is white-outlined via `stroke:#fff` + `fillAfterStrokeEnabled` (stroke first, blue fill on top). The drag ghost uses the same pair, and the reshape handles write endpoint moves to **all** arrows in the group (`find("Arrow")`, not `findOne`) so halo and ink can''t drift apart; an old draft''s single-arrow measurement hydrates/reshapes unchanged, it just keeps the halo-less look. `collectLineSegments` filters walls by `kind === "line"`, so the halo can''t affect flood fill. `node --check` clean; 61/61 tests.
- **Demo mode now replays a captured real session (2026-07-07)** — user hand-filled the whole form as fictional applicant "Mike Smith" (11506 Aaron Ave; patio cover / turf conversion / desert plants; back-yard photo answers; a hand-drawn plot — 1,025 painted cells + 30 plant stamps at bearing ~39.44°; typed signature) and asked for that to become the demo data. The draft + IndexedDB attachments were exported from the live localhost session (in-page JS: canvas-downscale each image to ≤1400 px JPEG q0.8, then POST draft+images to a throwaway local Node receiver on 127.0.0.1:8787 — no downloads, nothing left the machine) into **`assets/demo/`**: `demo-data.json` (~28 KB, incl. the full serialized plot restorePlot() takes verbatim) + 11 JPEGs (~3 MB total, down from ~34 MB of pasted PNGs). demo-mode.js rewritten as a thin player for that capture: lazily fetches demo-data.json on first toggle, attaches same-origin image Files per improvement row / photo shot, restores the real drawn plan instead of the old two illustrative rectangles, and drops the Unsplash CDN dependency + `DEMO_NAMES`/`DEMO_IMPROVEMENTS`/`DEMO_PARCEL` constants entirely (parcel ring/bearing live in the JSON now; placeholder-canvas fallback kept per image). A `console.warn` flags a stale capture if the rebuilt grid dims ever drift from the captured cols/rows. `node --check` clean.

- **Material library + demo-toggle fix (2026-07-07, ad hoc)** — two user requests. (1) The demo banner showed *both* a native tick box and the custom toggle track: the global `input[type="checkbox"]` rule (equal specificity, later source order) was overriding the `.demo-toggle input` visually-hidden rule's `position:absolute` (making `clip` inert) and 1px sizing. Fixed by qualifying the selector to `.demo-toggle input[type="checkbox"]` — one toggle switch remains. (2) The Draw step now starts from a **material-library modal** (`#material-library-modal`): a multi-select grid of the prebuilt `PALETTE` swatches plus the user's customs, with the custom-material creator folded inside it (the old inline `#palette-pop` popover is gone, and the perceived-brightness > 220 rejection on custom colors was removed at the user's request). It auto-opens when the Draw step is entered with an empty chip row (deferred a tick so demo-mode's `restorePlot` wins; `restorePlot` also force-closes it), and "Use these materials" (disabled until ≥1 picked) commits the picks to the new `selectedMaterials` chip-row state. Chips each carry a remove "×" (`removeSelectedMaterial` — painted cells keep rendering via `PALETTE_MAP` and still reach the print legend; removing the last chip reopens the library), "+ Materials" reopens the library any time, and a paint/rect/fill press with no resolvable material reopens it too. On "Done — use this plan" the chip row now **stays visible as a read-only legend** (`.plot.is-locked #palette` gets `pointer-events:none` + a "Legend:" prefix instead of `display:none`). Persisted additively as `plot.selectedMaterials`; drafts without it (incl. the demo capture) restore with everything selected, the pre-library row. `node --check` clean on all four touched modules, `node --test` 61/61. Needs the user's eyes: the first-run modal flow, chip remove/re-add, the locked legend, and a demo-mode toggle round trip.

- **Library presets expanded (2026-07-07, follow-up)** — `PALETTE` grew from 7 to 13 built-ins, regrouped ground covers → hardscape → water → structures: added Ground Cover Plants (dark-green tufts), Decomposed Granite (tan dots — the `dots` hatch finally has a built-in user), Gravel / Rock (gray speckle, darker than Concrete's), Pavers (terracotta brick), Wood Deck (brown diag), and Pool / Spa (`#2f7d9c` — deliberately under the brightness-110 mark so `textureMarkStyle` gives it white marks — with a new tileable `waves` hatch in `TEXTURES`, which the custom-material texture picker inherits automatically). Existing ids/labels untouched, so the demo capture and old drafts are unaffected; pre-library drafts now default-restore with all 13 chips.

- **Locked legend shows used materials only (2026-07-07, follow-up)** — on "Done — use this plan" the chip row now re-renders as a legend of what's actually painted (`renderPaletteChips` branches on `plotConfirmedFlag`, re-run from `setPlotConfirmed`, which every flag mutation already funnels through — Done, Make changes, Clear plan, parcel rebuild, draft restore). The used set matches the print legend's (`PALETTE` + retired + customs filtered by painted cells), so a painted-then-removed swatch still gets named; unused swatches hide, and an annotation-only plan hides the bare "Legend:" row entirely (`#palette.is-empty`). Unlocking restores the full interactive row.

- **Builder quick-tutorial video, trigger-timing fix (2026-07-07, ad hoc).** A short screen-recording walkthrough (`assets/builder-quick-tutorial.mp4`) plus a modal (`#tutorial-modal`, index.html) and a replayable "Tutorial" header button (`#tutorial-replay-btn`) were added to the Site/Plot Plan card in an earlier part of this session, but the auto-show-once call (`maybeShowTutorialOnce()`) had been wired into `startBuilder()` — firing on the step 1→2 "Build a plan" click, before the user had even seen a map. User expected it after finishing orientation instead. Moved the call into `showStep()`'s `n === 4 && currentStep === 3` branch (the same "just finished Orient" condition the aerial-backdrop fetch already uses), so it now fires on the real Orient→Draw transition (`#step3-next` "Continue") and is naturally skipped by demo-mode's/the calibration overlay's direct `restoreParcelFromDraft`+`showStep(4)` jumps, which never pass through step 3. Also: the existing `?reset`/`#reset` dev escape hatch (app.js) purged the draft but left the tutorial's `localStorage["fairwayCanyonArcTutorialSeen"]` seen-flag untouched, so a reset reload still silently suppressed the modal — user flagged this as annoying for repeat testing. Fixed with an independent `?reset`/`#reset` check at the top of map-wizard.js's tutorial section that clears the same key, so one URL param now gives a fully fresh reload (draft + tutorial) without touching devtools. `node --check` clean on app.js/map-wizard.js. Needs the user's eyes: confirm the video actually pops up on a real Orient→Continue click, and that `?reset` replays it.
